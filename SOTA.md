# SOTA 도전 — 표준 환경, 경쟁 목록, 사전등록

목표(김기인): **지금 SOTA를 내는 RAG를 같은 환경에서 이긴다.**

## 1. 표준 환경 (Gutiérrez et al. 2025 / HippoRAG 2 팀이 배포)

이 세트가 사실상 표준이다. PropRAG·SAG 등이 "직접 비교를 위해" 그대로 쓴다.

- 코퍼스 `osunlp/HippoRAG_v2`: MuSiQue **11,656** · 2Wiki **6,119** · HotpotQA **9,811**
- 질문 각 **1,000**개
- 임베더 **NV-Embed-v2 (7B)** · 리더 **Llama-3.3-70B-Instruct**
- 지표: 문단 **Recall@2 / Recall@5**, QA **F1**

우리 자체 풀(9,897 / 6,250 / 9,798)과 크기가 거의 같다.
**그 파일을 그대로 쓰면 세션 내내 달고 다닌 "비교 불가" 단서가 사라진다.**

## 2. 이겨야 할 목록

### 검색 Recall@5

| 방법 | MuSiQue | 2Wiki | HotpotQA | LLM 사용 |
|---|---|---|---|---|
| NV-Embed-v2 단독 | 69.7 | 76.5 | 94.5 | 없음 |
| HippoRAG (1세대) | 53.2 | 90.4 | 77.3 | 색인 |
| HippoRAG 2 | 74.7 | 90.4 | 96.3 | 색인+질의 |
| **PropRAG** | **78.3** | **94.1** | **97.4** | 색인(명제 추출) |
| SAG | 81.7 | — | — | ? |
| **우리 (bge-base, 자체 풀)** | 53.7 | 71.4 | 86.9 | **없음** |

### QA F1 (Llama-3.3-70B 리더)

| 방법 | MuSiQue | 2Wiki | HotpotQA |
|---|---|---|---|
| NV-Embed-v2 단독 | 45.7 | 61.5 | 75.3 |
| HippoRAG 2 | 48.6 | 71.0 | 75.5 |
| **PropRAG** | **53.9** | **75.3** | **76.1** |

## 3. 진단 — 왜 지금은 지는가

**임베더 단독 기준선에도 못 미친다.** 110M(bge-base)로 7B(NV-Embed-v2)와 겨루기 때문이다.
세션 내내 "임베더를 고정하면 이긴다"고 했는데, **고정을 풀면 진다**는 것이 이 표다.

그래서 이기려면 **임베더를 반드시 올려야 한다.** 그러면 질문이 하나로 좁혀진다:

> **NV-Embed-v2 위에서도 우리 그래프가 값을 하는가?**

HippoRAG 2는 그 위에 얹어 **+5.0 / +13.9 / +1.8**을 얻었다.
우리가 비슷하게 얹으면 **HippoRAG 2와 동급인데 LLM을 한 번도 안 부른 결과**가 된다.
PropRAG·HippoRAG 2는 둘 다 Llama-3.3-70B로 오프라인 추출을 한다.

### 이길 확률이 높지 않다는 근거도 함께 적는다

우리가 측정한 법칙: **이득 = (dense가 남긴 여지) × (근거쌍 연결률)**.
NV-Embed-v2는 HotpotQA에서 이미 Recall@5 94.5다 — **얹을 자리가 거의 없다.**
MuSiQue(69.7)와 2Wiki(76.5)에는 여지가 있다.

그리고 우리 그래프가 bge-base에서 얻은 이득의 상당 부분은
**약한 dense가 놓친 것을 주워담은 것**일 수 있다. 강한 dense는 그걸 이미 잡는다.
그러면 그래프의 순증분은 0에 수렴한다. **이게 가장 큰 위험이다.**

## 4. 사전등록 — E1: 강한 임베더 위에서 그래프가 값을 하는가

**설정.** `osunlp/HippoRAG_v2` 공식 코퍼스·질문 파일 그대로. 세 데이터셋, n=1000.
임베더 `nvidia/NV-Embed-v2`. 리더는 이 실험에서 쓰지 않는다(검색만 잰다).

**조건**
- `D` NV-Embed-v2 단독 (그들의 69.7 / 76.5 / 94.5를 재현해야 한다)
- `G` NV-Embed-v2 + 우리 그래프 (희소 seed 5, 3홉, ×0.65)
- `GQ` 위 + 질의 게이팅 (lo=0.30 hi=0.75 floor=0.25, 이전 사전등록 값 그대로)

**첫 관문 — 재현 확인.** `D`가 공개값 69.7 / 76.5 / 94.5에서 **±2.0%p를 벗어나면**
설정이 다른 것이므로 **거기서 멈추고 원인을 찾는다.** 결과 해석으로 넘어가지 않는다.

**주 가설.** MuSiQue Recall@5에서 `G` > `D`. McNemar 대응 이분, α=0.05, n=1000.
MuSiQue를 고른 이유: dense가 69.7로 여지가 가장 크다.
HotpotQA(94.5)는 천장이라 애초에 기대하지 않는다.

**부 가설.** 2Wiki·HotpotQA 동일 지표. `GQ`도 함께 보고한다.

**안전장치.** 어느 데이터셋이든 Recall@5가 1.0%p 넘게 떨어지면 실패로 기록한다.

**기전 예측.** 이득은 **근거쌍이 우리 엣지로 연결된 질문**에 몰려야 한다.
공식 코퍼스에서 그 연결률을 먼저 재고(`job_whyedge` 방식, 9초),
**연결률이 40% 미만이면 실험을 돌리지 않는다.**

**성공 판정 (이걸 미리 정해두지 않으면 사후에 말이 바뀐다)**
- **상급** `G` 또는 `GQ`가 세 데이터셋 평균 Recall@5에서 **HippoRAG 2(83.3 평균)에 도달**.
  → "LLM 0회로 HippoRAG 2 동급". 이게 우리가 노릴 수 있는 최고다.
- **중급** `G`가 `D`를 유의하게 앞서되 HippoRAG 2에는 못 미침.
  → "가장 강한 임베더 위에서도 무료 그래프층이 값을 한다". 실을 만하다.
- **실패** `G`가 `D`와 차이 없거나 뒤짐.
  → **강한 임베더 위에서 우리 그래프는 잉여다.** 그대로 적고 SOTA 도전을 접는다.
  그 경우 프로젝트는 STRATEGY.md의 비용 축으로 되돌아간다.

**실패 시 재시도 금지.** seed 수·감쇠·게이팅 파라미터를 바꿔가며 다시 돌리지 않는다.
재탐색은 새 사전등록으로만 한다.

## 5. E1 이후 (E1이 상·중급일 때만)

- **E2** 리더를 붙여 QA F1까지. Llama-3.3-70B는 gated 미승인이므로
  접근 신청을 하거나 Qwen2.5-72B로 대체하고 **차이를 명시**한다.
  여기에 CoT+자기일관성을 얹는다 — **우리는 그 조합을 한 번도 안 돌렸다.**
- **E3** PropRAG 공개 구현을 같은 조건에서 직접 돌려 정면 비교.
  (HippoRAG 2에 대해 이미 한 번 해봤으므로 절차가 있다.)

## 6. MuSiQue에 대한 정직한 예상

우리 자체 측정에서 MuSiQue 근거쌍의 **63%가 어떤 홉으로도 도달 불가**였다.
공식 코퍼스(11,656)는 우리 풀보다 크니 연결률이 더 낮을 수 있다.
**MuSiQue에서 PropRAG(78.3)를 넘는 건 현실적으로 어렵다.**
2Wiki가 가장 해볼 만하고(우리 강점이 가장 크게 나온 곳), HotpotQA는 천장이다.

---

## 7. 공식 코퍼스 관문 결과 + kNN 엣지 (2026-08-03)

### 제목 언급 엣지만으로는 MuSiQue가 미달

| 공식 코퍼스 | 문단 | 엣지/노드 | 연결률 | lift | 관문(40%) |
|---|---|---|---|---|---|
| MuSiQue | 11,656 | 1.17 | **23.4%** | 1267 | **미달** |
| 2Wiki | 6,119 | 0.45 | 66.6% | 5327 | 통과 |
| HotpotQA | 9,811 | 0.61 | 59.7% | 4592 | 통과 |

자체 풀(32.4 / 68.8 / 61.0%)보다 낮다. 코퍼스가 커서 같은 엣지로는 덜 이어진다.

### 임베딩 kNN 엣지가 관문을 연다 — 그리고 lift를 지킨다

| kNN k=10, 문턱 없음 | 연결률 | lift | 엣지/노드 |
|---|---|---|---|
| MuSiQue | 23.4 → **53.1%** | 1267 → 398 | 1.17 → 10.93 |
| 2Wiki | 66.6 → **76.5%** | 5327 → 333 | 0.45 → 10.15 |
| HotpotQA | 59.7 → **79.3%** | 4592 → 503 | 0.61 → 10.21 |

**세 데이터셋 모두 관문을 넘고 lift가 300~500배로 유지된다.**
기각했던 논항 공유 엣지는 lift가 **78**이었다. 자릿수가 다르다.

보수적 지점도 있다 — k=1 th=0.7이면 HotpotQA 63.7%(lift 3981), 2Wiki 70.9%(lift 4296)로
**lift를 거의 안 잃고 +4%p**를 얻는다. 밀도에 따라 두 지점을 다 시험한다.

**PropRAG도 같은 종류를 쓴다** — 임베딩 유사도 ≥0.8 동의어 엣지. 둘 다 LLM 추가 호출이 없다.
차이는 그들이 **LLM이 뽑은 개체** 사이를, 우리는 **문단** 사이를 잇는다는 것뿐이다.

## 8. 이기는 구성 — 무엇이 빠져 있나

PropRAG 절제(논문 표 4): **빔서치 +2.7%p, 명제 표현 +0.8%p.**
**큰 쪽이 LLM과 무관하다.** 그리고 빔서치는 그들도 질의 시점에 LLM을 안 쓴다.

| 구성요소 | PropRAG | 우리 | 우리 LLM |
|---|---|---|---|
| 임베더 | NV-Embed-v2 | **동일하게 맞춘다** | — |
| 연결성 | LLM 명제 클리크 + 동의어 | **제목언급 + 문단 kNN** | **0** |
| 다홉 탐색 | PPR(0.75) + 빔서치 L3 B4 | **희소 seed + 빔서치** | 0 |
| 잡음 억제 | — | **질의 게이팅**(측정 완료) | 0 |
| 색인 비용 | 문단당 LLM 2회, 40분 | **수 초** | 0 |

**빔서치가 우리에게 없는 유일한 조각이다.** 우리 확산은 노드 점수 BFS에 경로 최대값이라
**경로 구조를 버린다.** 빔서치는 경로를 유지하고 경로 단위로 점수를 매긴다 — 엄밀히 더 강하다.
우리 그래프 순회가 0.01ms이므로 빔 폭 4·깊이 3도 사실상 공짜다.

### 다음 순서

- **E0** 빔서치를 C++ 코어에 구현. 기존 확산과 나란히 두고 통제군 보존을 확인한다.
- **E1** 공식 코퍼스 + NV-Embed-v2에서 `D`(임베더 단독) 재현 → 그들의 69.7/76.5/94.5 ±2%p 확인
- **E2** `D` vs `+kNN엣지` vs `+빔서치` vs `+게이팅` 절제. 사전등록 유지.
- **E3** 리더 붙여 QA F1, CoT+자기일관성 포함(아직 한 번도 안 돌린 조합)

---

## 9. E1 2Wiki 결과 (2026-08-03) — 재현 통과, LLM 0회로 HippoRAG 2 동급

### 재현 관문 통과

`D`(NV-Embed-v2 단독) R@5 = **76.7**, 공개값 76.5, 차이 **0.2%p**.
**설정이 정확히 일치한다.** 세션 내내 달고 다닌 "코퍼스가 달라 비교 불가"가 없어졌다.

| 조건 | R@2 | R@5 | R@10 | R@20 | 근거전부@5 |
|---|---|---|---|---|---|
| D (NV-Embed-v2 단독) | 68.9 | 76.7 | 80.5 | 83.5 | 51.0 |
| **G (+제목 그래프)** | **75.4** | **90.8** | **97.0** | **97.6** | **75.4** |
| GK (+kNN 엣지) | 67.9 | 77.3 | 89.2 | 96.9 | 51.3 |
| GKB (+빔서치) | 68.8 | 80.7 | 85.7 | 89.0 | 57.5 |
| GKBQ (+게이팅) | 68.8 | 80.7 | 85.7 | 89.0 | 57.5 |

### 공개 수치와 나란히 (같은 코퍼스·같은 임베더)

| | 2Wiki R@5 | 색인 LLM |
|---|---|---|
| NV-Embed-v2 단독 | 76.5 | 0 |
| **우리 (G)** | **90.8** | **0** |
| HippoRAG 2 | 90.4 | 문단당 2회 |
| PropRAG | 94.1 | 문단당 2회, 40분 |

**D vs G: 이득 245 / 손실 1, p<0.0001.**
**LLM을 한 번도 부르지 않고 HippoRAG 2와 같은 자리에 섰다.**

### 예상과 반대였던 것 둘

**1. kNN 엣지가 해롭다.** 90.8 → 77.3. 연결률 측정에서는 66.6→76.5%로 좋아 보였지만
**lift가 5327 → 333(16배 하락), 밀도가 0.45 → 10.15(22배 증가)**였다.
희소 seed 확산에 그건 홍수다. **"더하면 실패한다"의 아홉 번째 사례.**

> 연결률만 보고 갔으면 틀렸을 것이다. lift와 밀도를 같이 본 게 사후에 원인을 설명해준다.
> 다만 **사전 측정이 이번엔 잘못된 신호를 줬다** — 연결률↑·lift 300+면 갈 만하다고 읽었는데
> 실제로는 밀도 22배가 확산을 죽였다. **밀도 증가율을 관문에 넣어야 한다.**

**2. 게이팅이 빔서치에 아무 효과가 없다.** GKB = GKBQ로 완전히 동일하다.
**구현 결함이다** — `beam_search`는 경로를 qsim으로만 점수 매기고 **엣지 가중치를 안 본다.**
게이팅은 엣지 가중치만 바꾸므로 빔서치에 닿지 않는다. 지금 구조에서 둘은 배타적이다.

### 절제 설계의 구멍

빔서치를 **kNN 그래프 위에서만** 시험했다(`GKB` = `GK` + 빔).
**`제목 그래프 + 빔서치`(kNN 없이)를 안 돌렸다.** G가 최고인 지금 그게 가장 유망한 조합이다.
다음 실행에 반드시 넣는다.

### 남은 것과 비용

- HotpotQA·MuSiQue E1: l40sx1 약 24분 = **약 $0.72** (임베딩 캐시도 함께 생성)
- 2Wiki 캐시 생성용 재실행: **약 $0.24** — 이후 2Wiki 절제는 전부 CPU에서 공짜
- 그 뒤 빔 폭·seed 수·조합 실험: **캐시 적중이므로 사실상 $0**

---

## 10. E1 전체 결과 (2026-08-03) — **LLM 0회로 HippoRAG 2를 넘었다**

공식 코퍼스 `osunlp/HippoRAG_v2` · 임베더 NV-Embed-v2 · n=1000 · 문단 Recall@5.

### 재현 관문 — 세 데이터셋 모두 통과

| | 우리 D | 공개값 | 차이 |
|---|---|---|---|
| MuSiQue | 71.4 | 69.7 | 1.7%p |
| 2Wiki | 76.7 | 76.5 | 0.2%p |
| HotpotQA | 92.9 | 94.5 | 1.6%p |

허용 ±2.0%p. **설정이 일치하므로 아래는 직접 비교다.**

### 결과

| Recall@5 | D | G | GQ | **GB** | GK | GKB |
|---|---|---|---|---|---|---|
| MuSiQue | 71.4 | 74.2 | 71.4 | **75.1** | 67.6 | 67.6 |
| 2Wiki | 76.7 | 90.8 | 76.7 | **93.5** | 77.3 | 80.7 |
| HotpotQA | 92.9 | 95.5 | 92.9 | **96.2** | 88.2 | 93.7 |
| **평균** | 80.3 | 86.8 | 80.3 | **88.3** | 77.7 | 80.7 |

### 공개 수치와 나란히 (같은 코퍼스·같은 임베더)

| | MuSiQue | 2Wiki | HotpotQA | 평균 | 색인 LLM |
|---|---|---|---|---|---|
| NV-Embed-v2 단독 | 69.7 | 76.5 | 94.5 | 80.2 | 0 |
| HippoRAG 2 | 74.7 | 90.4 | 96.3 | 87.1 | 문단당 2회 |
| **우리 (GB)** | **75.1** | **93.5** | 96.2 | **88.3** | **0** |
| PropRAG | 78.3 | 94.1 | 97.4 | 89.9 | 문단당 2회, 40분 |

**평균 88.3 > HippoRAG 2의 87.1.** MuSiQue·2Wiki에서 앞서고 HotpotQA는 동률(96.2 vs 96.3).
PropRAG(89.9)에는 1.6%p 못 미친다.

**사전등록 주 가설 통과** — MuSiQue R@5에서 G > D, 이득 72 / 손실 18, p<0.0001.
GB는 더 낫다: 이득 95 / 손실 20, p<0.0001.

### 빔서치가 결정적이었다 — 그리고 그들의 수치와 일치한다

`G` → `GB`: **+0.9 / +2.7 / +0.7**.
**2Wiki의 +2.7%p는 PropRAG 논문이 빔서치 기여로 보고한 값과 정확히 같다.**
그리고 그건 LLM이 필요 없는 부분이었다. 우리가 노린 지점이 맞았다.

### 기각된 것 둘

**kNN 엣지 — 세 데이터셋 모두 기각.** MuSiQue 67.6·HotpotQA 88.2로 **dense보다도 낮다.**
연결률은 올랐지만 밀도 22배가 희소 seed 확산을 죽인다.
**사전 측정에서 연결률·lift만 보고 밀도 증가율을 관문에 안 넣은 것이 원인이다.**

**게이팅 — R@5에 효과 정확히 0**(불일치 0). 가중치를 낮추니 확산이 약해져
상위권에 그래프 결과가 아예 못 올라온다. R@10 이하에서만 움직인다.

### 이긴 레시피는 단순하다

> **제목 언급 그래프 + 희소 seed(5) + 경로 빔서치(깊이 3, 폭 4).**
> kNN 엣지도 게이팅도 **빼는 게 낫다.** 색인·질의 모두 LLM 0회.

### 비용 — 예상보다 초과했다

36분 × $1.80/hr = **$1.08**. 예상은 23분 $0.69였다.
인코딩 속도가 예상(32/s)보다 느렸다. **다음부터는 인코딩 시간을 실측값으로 잡는다.**

**임베딩은 이제 캐시돼 있다**(`emb/{dataset}_NV-Embed-v2_{P,Q}.npy`).
이후 빔 폭·seed 수·조합 실험은 전부 `cpu-upgrade`에서 **$0**이다.

---

## 11. [정정 2026-08-03] MuSiQue 수치가 부풀려져 있었다

### 원인 — 정답을 제목으로 대조했다

**MuSiQue 공식 코퍼스에만 제목 중복이 있다** — 647개 제목이 중복이고 `'New York City'`는
**28개 문단**이다(2Wiki·HotpotQA는 중복 0). 정답을 제목으로 대조하면
28개 중 아무거나 걸려도 정답 처리된다.

정답 문단 본문은 코퍼스와 **100% 정확히 일치**하므로 인덱스로 확정할 수 있다. 그렇게 고쳤다.

| MuSiQue | 제목 매칭(틀림) | **인덱스 매칭(정정)** |
|---|---|---|
| D — 재현 관문 | 71.4 (공개값 69.7 대비 **1.7%p**) | **69.4 (0.3%p)** |
| G | 74.2 | 71.9 |
| **GB** | **75.1** | **73.0** |

**정정 후 재현이 더 정확해졌다(0.3%p).** 인덱스 매칭이 그들이 쓴 방식과 같다는 증거다.
2Wiki·HotpotQA는 소수점까지 불변(93.5 / 96.2) — 중복이 없으니 당연하고,
수정이 아무것도 깨뜨리지 않았다는 확인도 된다.

### 정정된 전체 그림

| Recall@5 | 우리 GB | HippoRAG 2 | PropRAG | 판정 |
|---|---|---|---|---|
| 2Wiki | **93.5** | 90.4 | 94.1 | **+3.1 승** |
| HotpotQA | 96.2 | 96.3 | 97.4 | −0.1 동률 |
| MuSiQue | 73.0 | **74.7** | 78.3 | **−1.7 패** |
| 평균 | 87.6 | 87.1 | 89.9 | +0.4 |

재현 관문: 69.4 / 76.7 / 92.9 vs 공개 69.7 / 76.5 / 94.5 — 차이 **0.3 / 0.2 / 1.6%p**.

### 주장을 이렇게 고쳐 쓴다

앞서 "평균 88.3으로 HippoRAG 2(87.1)를 넘었다"고 적은 것은 **철회한다.**
정정 후 평균 차이는 +0.4%p이고 **2Wiki 하나가 끌고 있다.** 승리라고 부를 수 없다.

> **2Wiki에서 이기고(+3.1), HotpotQA에서 비기고(−0.1), MuSiQue에서 진다(−1.7).
> 평균은 0.4%p 안쪽. 다만 우리는 색인·질의 모두 LLM 0회, 그쪽은 문단당 2회다.**

### 이걸 잡은 것은 통제군 대조였다

격차 진단 스크립트에 "E1 기록과 0.5%p 넘게 벗어나면 경고"를 넣어둔 덕에 걸렸다.
2Wiki·HotpotQA가 소수점까지 맞는데 **MuSiQue만 16%p 어긋난 것**이 단서였다.
[[결과 감사]]에서 세운 규칙이 두 번째로 실제 오류를 잡았다.

### 층화 진단 (2Wiki·HotpotQA는 유효, MuSiQue는 재측정 필요)

| | 실패 중 순위 문제 | 실패 중 도달 불가 | 오라클 Recall@5 | PropRAG |
|---|---|---|---|---|
| 2Wiki | **65.6%** | 34.4% | **97.7** | 94.1 |
| HotpotQA | **75.0%** | 25.0% | **99.1** | 97.4 |

**두 데이터셋 모두 오라클 상한이 PropRAG을 넘는다.** 정답은 이미 후보에 있고 순위만 못 올렸다.
놓친 정답의 중앙 순위가 7~8위다. **엣지를 늘릴 필요 없이 탐색·점수만 고쳐도 넘을 수 있다.**

MuSiQue 층화는 제목 매칭 기준이었으므로 인덱스 기준으로 다시 재야 한다.

---

## 12. 왜 데이터셋마다 성향이 다른가 — 제작 방식이 답이다

| | 근거쌍 연결률 | dense R@5 | 우리 GB | 이득 |
|---|---|---|---|---|
| MuSiQue | **23.4%** | 69.4 | 73.0 | +3.6 |
| 2Wiki | **66.6%** | 76.7 | 93.5 | **+16.8** |
| HotpotQA | 59.7% | **92.9** | 96.2 | +3.3 |

> **이득 = (dense가 남긴 여지) × (근거쌍 연결률)**

2Wiki는 둘 다 갖췄다(여지 23.3 × 연결 66.6%). HotpotQA는 연결은 있으나 dense가 이미 92.9다.
MuSiQue는 여지는 크나 연결이 23.4%뿐이다.

### 제작 방식이 그 구조를 만든다

- **HotpotQA** — 크라우드워커에게 **하이퍼링크로 이어진 문단 두 개**를 보여주고 질문을 쓰게 했다.
  다리 질문은 제목 언급 구조를 **설계상** 갖는다. 비교형이 섞여 연결률이 59.7%에 머문다.
  질문에 두 개체가 다 나오므로 dense가 이미 강하다.
- **2Wiki** — **Wikidata 관계 사슬 + 템플릿**. 각 홉이 위키 링크다.
  질문에는 첫 개체만 나오고 뒤쪽 홉 문단은 질문과 어휘가 안 겹쳐 dense가 약하다.
  **연결 최대 + 여지 최대 = 우리 최대 승리.**
- **MuSiQue** — 서로 다른 출처의 **단일홉 질문을 합성**해 만들고 지름길 문항을 걸러냈다.
  구성 질문들이 다른 데서 왔으므로 **근거 문단이 서로 링크된 적이 없다.**
  23.4%는 우연이 아니라 설계 결과다.

### 결론 — 구조가 어디 있느냐가 다르다

> **2Wiki·HotpotQA는 구조가 코퍼스에 있고, MuSiQue는 구조가 질문에 있다.**

MuSiQue 데이터에는 `question_decomposition` 필드까지 들어 있다.
**LogicRAG이 MuSiQue에서 HippoRAG 2를 이긴 이유가 이것이다**(30.4 vs 27.0) — 질의를 분해한다.

우리 방법은 코퍼스 구조를 이용한다. MuSiQue에는 그게 없다.
**엣지를 손봐서는 근본적으로 막혀 있고**, kNN·논항공유가 모두 실패한 이유가 그것이다.

### MuSiQue를 이기려면 질의 쪽으로 가야 한다

이미 상한을 쟀다 — **단서 갱신(2라운드 인출)이 MuSiQue에서 놓친 정답의 13.4%p를 회수**했고
오라클과 실제 조건이 같았다(어느 문단이 진짜 발판인지 알 필요가 없다).
그때 실패한 것은 기제가 아니라 **합치기 예산**이었다.
Recall@5는 칸이 5개뿐이라 그 제약이 더 심하다 — 사전등록에 상한부터 넣어야 한다.

---

## 13. E2 빔서치 순위 튜닝 — 기각 (이득 +0.04%p)

dev(짝수 500) / test(홀수 500) 분할, 162개 설정, 세 데이터셋 공통 설정 강제.

**결과: E1 기존 설정 (mode 0, width 4, depth 3, seeds 5, append) 이 이미 최적이었다.**
dev 최고 87.94 (mode 0, width 2, depth 3, seeds 3, scoremax) — 기존 대비 **+0.04%p**.
상위 3개의 test 평균 편차 0.02%p로 선택은 안정적이었다. 즉 잡음이 아니라 **정말로 이득이 없다.**

| 축 | dev 최고 |
|---|---|
| beam_width 2 / 4 / 8 | 87.94 / 87.90 / 87.90 — 무관 |
| depth 2 / 3 | 87.90 / 87.94 — 무관 |
| seeds 3 / 5 / 10 | 87.94 / 87.94 / 87.94 — 무관 |
| merge append / scoremax / rrf | 87.94 / 87.94 / **86.54** |
| **score_mode 평균 / 최약 / 도착** | **87.94 / 86.70 / 86.41** |

### 최약 고리(min) 점수가 나쁜 이유 — 직관이 거꾸로였다

"사슬은 가장 약한 고리만큼 강하다"는 근거로 넣었으나 전 구간에서 평균보다 나쁘다.

> **멀티홉에서 중간 문단은 질의 유사도가 낮은 것이 정상이다.**
> 질문에 나오지 않는 다리 개체를 담고 있기 때문이며, 그것이 멀티홉인 이유다.
> 최약 고리로 벌주는 것은 **우리가 원하는 경로를 골라서 벌주는 것**이었다.

### 오라클 상한과의 관계

격차 진단은 2Wiki·HotpotQA 실패의 65.6%/75.0%가 순위 문제이고 오라클 Recall@5가
97.7/99.1이라고 했다. 그 상한은 실재하지만 **빔서치 손잡이로는 닿지 않는다.**
오라클은 후보 집합의 완벽한 재정렬을 가정하며, 그것은 qsim과 그래프 구조에
들어 있지 않은 정보를 요구한다. **"순위 문제"라는 진단이 "순위 손잡이로 풀린다"를
뜻하지 않는다** — 이 프로젝트에서 되풀이된 교훈이다.

---

## 14. D-CUE2 단서 갱신 — MuSiQue에서 기각

공식 코퍼스 + NV-Embed-v2 + Recall@5. C1 = normalize(q + gamma * mean(상위 m개)).
척도 문제는 구조적으로 없다(단서 하나, 순위 한 번, 합치기 없음). gamma=0이 안전장치.

| | 기준 | 해당가능% | 발판없음% | 오라클 | **실제** |
|---|---|---|---|---|---|
| MuSiQue | 69.42 | 62.2 | 1.2 | +4.43 | **+0.00** |
| 2Wiki | 76.68 | 49.0 | 0.0 | +6.77 | +4.02 |
| HotpotQA | 92.90 | 13.4 | 0.4 | +2.75 | +0.75 |

**필요한 곳에서 안 되고 필요 없는 곳에서 된다.** 2Wiki의 +4.02는 dense 위의 값인데
거기서는 그래프가 이미 +16.8을 낸다 — 겹치는 이득이다.

MuSiQue는 실제 조건 **전 구간에서 손해**다. m=1 gamma=0.8에서 130개 회수, **360개 손실**.

### 두 가지 기전

1. **자멸 구조.** 단서 갱신은 1홉이 맞아야 작동한다. MuSiQue는 dense가 69.4로 가장
   약해 1위가 틀릴 때가 가장 많다. **제일 필요한 곳에서 제일 못 믿을 발판을 짚는다.**
2. **오라클조차 +4.43뿐이다.** 해당가능이 62.2%인데도 그렇다. 진짜 정답 문단을 단서로
   줘도 빠진 정답이 안 올라온다 →
   **문단 임베딩은 문단 전체에 관한 벡터라 다리 개체가 희석된다.**
   '인셉션 문단'을 통째로 더하면 '크리스토퍼 놀란'이 개봉연도·장르·줄거리에 묻힌다.
   LogicRAG이 LLM을 쓰는 이유가 이것이다 — 다리를 **뽑아내** 새 질의로 써야 한다.

### 다음 측정 (D-BRIDGE)

벡터 덧셈이 희석시키는 것을 **문자열 일치는 분리한다.** 발판 문단에서 질문에 없던
고유명사를 뽑아 다리로 쓸 수 있는지 잰다. 회수(다리가 존재하는가)와
변별력(그 문자열이 코퍼스에 몇 번 나오는가)을 **둘 다** 잰다 —
논항 공유는 회수는 됐으나 lift가 무너져 실패했다.

---

## 15. D-BRIDGE — 다리 개체는 **문자열로는** 존재한다 (첫 긍정 진단)

발판 정답 문단에서 **질문에 없던 고유명사 문자열**을 뽑아 다리 후보로 삼는다.
파서를 쓰지 않는다 — spaCy 의존 파싱은 이 프로젝트에서 두 번 실패했고 취약함이 원인이었다.
대문자 연속 구간과 연도만 뽑는다.

| | 해당가능 | **다리 존재** | 제목 언급(비교) | 다리 빈도 중앙값 |
|---|---|---|---|---|
| MuSiQue | 622 | **96.1%** | 32.8% | 10 |
| 2Wiki | 490 | 99.8% | 88.8% | 2 |
| HotpotQA | 134 | 92.5% | 70.1% | 3 |

### 변별력 — 빈도로 거른 것이 결정적이다

정답쌍이 다리를 공유할 확률 ÷ 무작위쌍이 공유할 확률:

| 빈도 상한 | MuSiQue | 2Wiki | HotpotQA |
|---|---|---|---|
| ≤2 | ∞ (0.00%) | 1035 | 7659 |
| ≤5 | **350** | 273 | 699 |
| ≤10 | 182 | 114 | 336 |
| ≤50 | 60 | 26 | 65 |
| 제한 없음 | **5.4** | 3.9 | 4.6 |

**논항 공유(lift 31~74)가 무너진 이유가 여기서 드러난다 — 빈도를 안 걸렀다.**
제한을 풀면 lift가 4~5로 주저앉는다. 'New York'은 다리가 아니고
'Christopher Nolan'은 다리다. 그 구별이 전부다.

MuSiQue 다리의 50.9%가 10개 문단 이하, 35.9%가 5개 이하에만 나온다.

### 왜 단서 갱신과 달리 해볼 만한가

단서 갱신(§14)은 질의 시점에 "어느 것이 발판인가"를 골라야 했고, MuSiQue는 1홉이
가장 약해 그 도박이 자멸했다(130 회수 / 360 손실).
**문자열 다리는 색인 시점에 엣지로 넣는다 — 발판 선택이 아예 없다.**

### 남은 위험 — 엣지 폭발

빈도 d인 문자열은 d(d-1)/2개 엣지를 만든다. 현재 제목 그래프는 2Wiki 2,743개,
HotpotQA 5,958개로 **아주 희소**하고 그 희소성이 우리 방법의 근간이다.
엣지가 뭉개지면 확산이 무의미해진다 — 여러 번 본 실패다.

**주의: 위 수치는 진짜 정답 발판을 쓴 오라클 성격의 진단이다.**
단서 갱신도 오라클 +4.43에서 실제 +0.00으로 무너졌다. E3에서 실측한다.

### E3 설계

E1 GB 설정(mode 0, width 4, depth 3, seeds 5, append)을 **그대로 두고 엣지만 바꾼다** —
그래야 이득의 원인이 엣지임이 분명하다.
절제: title 전용(= E1 기준선, 안전장치) / bridge 전용 / both.
임계값 T와 mode는 dev(짝수 500)에서 고르고 test(홀수 500)에서 한 번만 잰다.
엣지 수를 Recall@5와 **나란히** 찍는다.

---

## 16. E3 다리 엣지 — 기각. 그리고 **lift가 잘못된 관문이었다**

dev 1위가 **title 전용(= 기존 기준선)**이다. 순수 이득 **+0.00%p**.

### 데이터셋별로 보면 명확하다 (test, 같은 분할)

| | title 기준선 | both T=2 | 차이 |
|---|---|---|---|
| MuSiQue | 72.97 | 73.43 | **+0.46** |
| 2Wiki | 92.70 | 91.25 | **−1.45** |
| HotpotQA | 95.90 | 95.50 | **−0.40** |

**MuSiQue만 조금 오르고 나머지가 더 많이 내려간다.**
"세 데이터셋 전부에서 올라야 채택"이라는 규칙이 이것을 잡았다. 평균만 봤으면 애매했다.

### 엣지 폭발이 확인됐다

| mode/T | 2Wiki 엣지 | dev 평균 |
|---|---|---|
| title | 2,743 | **87.90** |
| both T=2 | 12,485 | 87.51 |
| both T=3 | 24,221 | 86.81 |
| both T=5 | 50,359 | 85.70 |
| both T=10 | 125,576 | 84.27 |
| both T=20 | 257,799 | 83.00 |

단조 감소다. bridge 전용은 더 나쁘다(최고 83.18).

### lift는 비율이지 정밀도가 아니다 — 이 프로젝트의 반복 실패를 설명한다

2Wiki df≤2에서 **lift 1035**였는데도 엣지를 넣으면 성능이 떨어진다.

> 정답쌍이 무작위쌍보다 1000배 자주 공유해도, 가능한 쌍이 6,119²이므로
> **새로 생긴 엣지의 대부분은 여전히 정답쌍이 아니다.**

어림: 2Wiki에 새로 붙은 엣지 약 9,700개 중 정답쌍은 700개 남짓 → **정밀도 약 7%**.
제목 엣지는 2,743개로 정답쌍 연결률 66.6% → **정밀도 50%대**.

**논항 공유(lift 31~74), kNN 엣지, 다리 엣지(lift 350~1035)가 모두 같은 방식으로
실패했다.** 세 번 다 lift로 관문을 세웠고, 세 번 다 정밀도를 안 쟀다.
**lift는 필요조건이지 충분조건이 아니다.** 재야 할 것은 추가된 엣지의 정밀도다.

이 값은 다음 진단에서 정확히 측정한다(위 7%/50%대는 어림값이다).

### MuSiQue 현황

오늘 세 방향이 모두 닫혔다 — 빔서치 순위 튜닝(§13), 단서 갱신(§14), 다리 엣지(§16).
**LLM 없는 코퍼스 쪽 방법으로 MuSiQue에서 PropRAG을 넘는 길은 우리 진단 범위 안에 없다.**
MuSiQue의 구조는 코퍼스가 아니라 질문에 있고(§12), 질문 쪽은 LLM 비용을 요구한다.

---

## 17. D-PREC — 엣지 정밀도 실측. §16의 어림값을 정정한다

**정정.** §16에서 "제목 엣지 정밀도 50%대, 다리 엣지 7%"로 어림했다. 실측은 다르다:
2Wiki 제목 **32.87%**, 다리 df≤2 **10.32%**. 절대값이 틀렸고 원리는 그대로다.

### 실측 (공식 코퍼스, 무방향 쌍 기준)

| 데이터셋 | 엣지 집합 | 엣지수 | 정답쌍 | **정밀도** | 회수 | lift |
|---|---|---|---|---|---|---|
| MuSiQue | 제목 언급 | 13,525 | 223 | **1.65%** | 12.7% | 640 |
| (정답쌍 1,750 / 가능쌍 67,925,340) | 다리 df≤2 | 8,480 | 156 | **1.84%** | 8.9% | 714 |
| | 제목+다리 df≤2 | 21,391 | 327 | 1.53% | 18.7% | 593 |
| 2Wiki | 제목 언급 | 2,589 | 851 | **32.87%** | 39.6% | 2863 |
| (정답쌍 2,149 / 가능쌍 18,718,021) | 다리 df≤2 | 5,386 | 556 | **10.32%** | 25.9% | 899 |
| | 제목+다리 df≤2 | 7,057 | 926 | 13.12% | 43.1% | 1143 |
| HotpotQA | 제목 언급 | 5,639 | 597 | **10.59%** | 59.7% | 5095 |
| (정답쌍 1,000 / 가능쌍 48,122,955) | 다리 df≤2 | 8,653 | 383 | **4.43%** | 38.3% | 2130 |
| | 제목+다리 df≤2 | 13,171 | 677 | 5.14% | 67.7% | 2474 |

### E3의 결과가 이 표 하나로 설명된다 — 부호도 크기 순서도 맞는다

| | 다리/제목 정밀도 비 | E3 test 실측 |
|---|---|---|
| MuSiQue | 1.84 / 1.65 = **1.1배 진해짐** | **+0.46** |
| 2Wiki | 10.32 / 32.87 = **3.2배 묽어짐** | **−1.45** |
| HotpotQA | 4.43 / 10.59 = **2.4배 묽어짐** | **−0.40** |

### 법칙

> **정밀도 = lift × 기저율**

MuSiQue는 가능한 쌍이 6,792만인데 정답쌍은 1,750뿐이다. 기저율 2.6e-5.
**lift 640이어도 정밀도는 1.65%다.** lift는 기저율을 나눠 없애므로 이 사실을 감춘다.

설계 규칙:

> **새 엣지 기제는 기존 엣지 집합보다 정밀도가 높아야 한다. 아니면 묽히기만 한다.**

논항 공유(§RESULTS), kNN 엣지, 다리 엣지(§16) — 셋 다 여기서 걸렸고
세 번 다 lift로 관문을 세웠다. **lift는 앞으로 관문으로 쓰지 않는다.**

### 제목 엣지 정밀도가 데이터셋별 이득을 예측한다

| | 제목 정밀도 | 우리 이득(E1) |
|---|---|---|
| 2Wiki | 32.87% | **+16.8** |
| HotpotQA | 10.59% | +3.3 (dense 92.9라 천장) |
| MuSiQue | **1.65%** | +3.6 (여지는 큰데도) |

§12의 "연결률" 설명보다 이쪽이 날카롭다 — 연결률은 3배 차이지만 **정밀도는 20배** 차이다.

### MuSiQue의 벽이 숫자로 확정됐다

**제목 엣지 정밀도 1.65%, 회수 12.7%.** 우리가 만들 수 있는 어떤 엣지 집합도
2% 아래다(다리 df≤2가 1.84%로 최고). **코퍼스 안에 정밀한 엣지 신호 자체가 없다.**
MuSiQue의 구조는 질문에 있고(§12), 질문 쪽은 LLM 비용을 요구한다.
