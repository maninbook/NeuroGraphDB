# NeuroGraphDB — 설계 문서

> 기존 RAG·VectorDB를 **유의미하게 앞서야** 의미가 있다. 그게 목표다.

## 아이디어 (2026-08-01, 사용자 원안)

### 1. 문장은 개념과 관계의 명제 구조다

문장 안의 단어들은 **논리와 기호학으로 연결된 개념**이다. 이건 명제(proposition) 개념이고,
초기 AI의 지식기반 학습 — 의미망, 프레임, 술어논리 — 이 다루던 것이다.
현재 GraphRAG는 (엔티티, 관계, 엔티티) 삼중항 추출에 머문다.
양화사·부정·양상·논항역할이 빠진, 납작한 표현이다.

**주장:** 명제 구조를 제대로 세우면 다중홉 추론이 검색이 아니라 **추론**이 된다.

### 2. 인출은 뇌인지과학으로 효율화된다 — 시냅스 강화

임베딩으로 단어를 차원 공간에 놓는 건 지나친 단순화다.
**함께 인출된 것들의 연결이 강화되고**(Hebb 1949), 강한 연결이
**빠른 공고화 + 빠른 인출**로 이어지는 저장소가 필요하다.

**주장:** 그래프가 사용에 따라 학습한다. 이건 HippoRAG와 실제로 다르다 —
HippoRAG의 그래프는 색인 후 **고정**이다. PPR을 돌릴 뿐 그래프가 변하지 않는다.

### 3. 이미 만든 것 — NeuroDB

`business/BrainDocs/neuro_db_c`. C++ 독자 엔진, `.synaptic` 영속 포맷.
absorb / connect / think(spreading activation) / infer(forward chaining) /
pulse(hebbian decay) / contradict. **"read가 DB를 수정한다"**가 핵심 원칙.

NeuroGraphDB = 이 엔진을 GraphRAG 과제에서 실제로 이기게 만드는 것.

---

## 현재 상태 — 왜 아직 못 이기는가

[NEURODB_DIAGNOSIS.md](NEURODB_DIAGNOSIS.md) 실측. 요약:

1. **임베딩에 의미가 없다.** "quantum physics experiment" 질의에 정답이 꼴찌로 나온다
2. **LSH가 전부를 seed로 잡아** 32노드 중 32개가 depth 0. spreading이 일어나지 않는다
3. 그래서 **헤비안은 작동하지만 영향력이 0**이다. 엣지 직결 노드를 101회 조회해도 top-10에 못 든다

코드가 틀린 게 아니라 **벡터 채널이 그래프 채널을 압도**한다.

## 이것이 곧 설계 원리다 — 패턴 분리 ↔ 패턴 완성

해마 색인 이론의 핵심은 두 힘의 길항이다.

| | 뇌 | NeuroGraphDB | 현재 상태 |
|---|---|---|---|
| **패턴 분리** | 치아이랑(DG). 비슷한 경험을 구별되게 저장 | 변별력 있는 표현 + **희소한 seed** | 전부 seed → 분리 0 |
| **패턴 완성** | CA3. 부분 단서로 전체 복원 | 엣지 순회가 실제로 일어남 | depth 0만 → 완성 0 |

HippoRAG가 되는 이유가 여기 있다. 엔티티 몇 개만 seed로 잡고 **나머지는 PPR이 한다.**
NeuroDB는 반대다. 벡터가 다 하고 그래프는 장식이다.

**현재 구현들이 이 길항을 명시적으로 조절하지 않는다 — 조사에서 찾은 빈틈이 이것이다.**

---

## 성공 기준 (사전등록)

"유의미하게 앞선다"를 미리 정의한다. 사후에 바꾸지 않는다.

**과제:** 멀티홉 QA — HotpotQA, 2WikiMultiHopQA, MuSiQue

**baseline은 전부 우리가 직접 돌린다.** 논문 공개 수치와 비교하지 않는다.
LLM·프롬프트·검색 예산·문서 집합이 다르면 비교가 아니다.
(기존 `benchmark_vs_logicrag.py`가 공개 수치를 쓰고 있어 교체 대상이다.)

| 비교 대상 | 왜 |
|---|---|
| BM25 | 어휘 검색 하한. 이걸 못 이기면 얘기가 안 된다 |
| 조밀 벡터 RAG | 실제로 사람들이 쓰는 것 |
| HippoRAG / HippoRAG 2 | 이 분야 SOTA이자 우리와 가장 가까운 접근 |

**판정:** 같은 문제 집합을 여러 방법이 푸는 **대응 설계**다.
McNemar 검정으로 판정한다. 통과율 두 개를 그냥 비교하지 않는다.
(BitBudget·slowcode에서 이 실수를 두 번 했다.)

**최소 검출 효과:** 사전에 정한다. 문항 수 n에서 검출 가능한 효과보다 작은 차이는
"차이 없음"으로 보고한다.

## 진행 순서 — 이 순서를 지켜야 한다

**0. baseline부터 측정한다.** 우리 것을 고치기 전에 이겨야 할 선이 어디인지 안다.
**1. 임베딩 교체.** 의미 변별이 되는 것으로. 이게 없으면 나머지가 다 무의미
**2. seed 희소화.** 상위 소수만 seed. 그래야 spreading이 일할 자리가 생긴다
**3. 헤비안 효과 측정.** 1·2 전에 재면 반드시 "효과 없음"이 나온다
**4. 명제 표현 도입.** 검색이 작동한 뒤에 얹어야 의미가 있다

3을 1·2보다 먼저 하면 안 된다. slowcode에서 겪은 함정과 같다 —
경합 구간이 없는데 측정하면 아무것도 안 나온다.

## 남은 위험

- **명제 표현이 진짜 이득인지 미검증.** 삼중항 대비 이득이 없을 수도 있다.
  4단계에서 절제(ablation)로 분리해야 한다
- **헤비안이 이득인지 미검증.** 사용 이력이 없는 1회성 QA 벤치마크에서는
  강화할 신호 자체가 부족하다. **반복 질의가 있는 평가 설계가 따로 필요하다** —
  이건 기존 멀티홉 QA 벤치마크로는 측정이 안 되는 축이다
- 색인 구축에 LLM 호출이 많이 든다. 비용 추정 먼저
