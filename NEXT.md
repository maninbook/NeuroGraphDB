# 다음 단계

지금까지 확정된 것과 실패한 것은 [RESULTS.md](RESULTS.md)에 전부 있다.
여기는 **다음에 뭘 할지**만 적는다. 우선순위 순.

## 원칙 — 아홉 번의 시도에서 나온 것

1. **더하지 말고 빼라.** 성공한 둘(희소 seed, 질의 게이팅)은 신호를 줄였고,
   실패한 일곱은 전부 뭔가를 더했다. 새 아이디어가 "무엇을 더하는가"라면 의심부터 한다.
2. **사전 측정 없이 실험하지 않는다.** 논항 공유와 ACT-R 합산은 GPU를 쓰기 전에 걸러졌다.
   다만 fan 할인처럼 **사전 측정을 통과하고도 실패**할 수 있다. 측정은 면제가 아니라 필터다.
3. **통제군이 기존 기록과 일치하는지 매번 확인한다.** 중복제거 버그를 이걸로 잡았다.
4. **검색 개선이 QA 개선이라고 가정하지 않는다.** 두 번 어긋났다.
   개선된 순위 구간과 파이프라인이 읽는 구간을 맞춰봐야 한다.

## A. 공개 이후 정리 (짧고 확실한 것)

- [ ] `max_depth=1`로 줄여도 결과가 같은지 확인. 1홉→3홉 도달률 차이가
      0.6 / 0.2 / 4.7%p뿐이라 깊이 3이 사실상 장식일 가능성이 높다.
      맞으면 알고리즘이 더 단순해지고 설명도 짧아진다.
- [ ] `job_*.py`가 20개 가까이 되고 상당수가 `load_pool`·`build_edges`를 복붙했다.
      공통 모듈로 빼되 **실험 결과가 바뀌지 않는지** 통제군 대조로 확인하며 옮긴다.
- [ ] HF Hub에 데이터셋 카드 하나 — 결과 JSON을 공개로 돌리고 재현 방법 첨부.

## B. 사전등록이 필요한 열린 질문

> **2026-08-02 갱신 — 재순위 계열은 소진됐다.**
> 상위 20개를 **완벽하게** 재배열해도 얻을 수 있는 최대치가
> HotpotQA 2.0%p / 2Wiki 1.1%p / MuSiQue 4.7%p다(`job_redundancy.py`).
> 측면 억제·MMR·다양성 항은 이 안에서 일부만 가져간다.
> **MuSiQue 질문의 61.2%는 상위 20개 안에 정답이 다 들어오지도 못한다.**
> 남은 병목은 순서가 아니라 **회수**다. 아래 항목을 그 기준으로 재배치했다.

### B1. 게이팅 이득 구간에서 QA 재기 (top-20)

게이팅은 R@20에서 +4.8%p, R@10에서 +1.0%p다. 그런데 QA를 top-10으로 쟀고 차이가 없었다
(6/8, p=0.79). **개선된 구간을 리더가 안 본다.**

top-20에서 재면 다를 수 있다. 다만 이건 **진 뒤에 유리한 조건을 고르는 것과 구분돼야 한다.**
사전등록에 넣을 것: 주 가설(MuSiQue EM, top-20), top-10도 함께 보고, 컨텍스트 길이가
두 배가 되는 비용을 명시. top-20이 이겨도 "top-10에서는 이득 없음"을 같이 적는다.

**상한이 이미 측정돼 있다** — 정답이 top-20엔 있는데 top-10엔 없는 질문이
2.0 / 1.1 / 4.7%다. 리더에게 20개를 주면 그 이상은 못 얻는다. 사전등록에 이 수치를
명시하고, MDE가 그 안에 들어오는지 먼저 확인한다. 안 들어오면 돌릴 이유가 없다.

### B2. 별칭 엣지에 낮은 가중치

별칭 엣지는 정확 제목 엣지보다 정밀도가 낮은데 가중치가 똑같이 1.0이다.
새로 연결된 질문에서 이득 53 / 손실 2로 기전은 완벽히 작동했고, 희석이 이겼다.
가중치를 낮추면(예: 0.5) 이득은 남기고 희석은 줄일 수 있다.

**이건 진 다음에 파라미터를 돌리는 것처럼 보인다는 게 문제다.** 하려면 가중치를
실행 전에 고정하고, 왜 그 값인지(정밀도 비율 추정)를 근거로 적어야 한다.

### B3. dense가 강할 때 그래프를 끄는 조건부 적용

같은 엣지가 2Wiki에서는 +38.6%p, HotpotQA에서는 −1.9%p다. 차이는 dense의 여유다.
질문별로 dense 점수 분포를 보고 그래프를 켤지 끌지 정할 수 있는지.

위험: 질문별 게이팅은 파라미터가 늘고 과적합하기 쉽다.
사전등록에 **홀드아웃 분할**을 명시해야 한다.

**우선순위 낮음** — 이것도 순서 조정이라 위의 상한(1~5%p)에 갇힌다.

### B4. 회수를 겨냥한 것만 남는다 (새 최우선)

재순위가 소진됐으므로, 의미 있는 개선은 **정답을 상위 20 후보에 넣는 것**뿐이다.
그러려면 근거쌍 연결률을 올려야 하는데, 지금까지 시도한 방법의 성적은 이렇다:

| 방법 | 연결률 변화 (MuSiQue) | lift | 검색 결과 |
|---|---|---|---|
| 제목 별칭 | 32.4 → 35.2% | 952 → 774 | 유의하지 않음 |
| 논항 공유 | 32.4 → 20.5% | 952 → 78 | 사전 측정에서 기각 |

별칭만이 유일하게 연결률을 올렸다. **B2(별칭 가중치 낮추기)가 지금 유일하게
회수를 겨냥한 열린 항목이고, 그래서 우선순위가 가장 높다.**

새 아이디어를 볼 때 기준은 하나다 — **근거쌍 연결률을 올리면서 lift를
900 이상으로 유지하는가.** 그 둘을 동시에 못 하면 돌릴 필요가 없다.
`job_argmeasure.py`가 9초에 답을 준다.

### B5. 정직한 가능성 — MuSiQue는 우리 방법의 대상이 아닐 수 있다

MuSiQue 근거쌍의 63%는 **어떤 홉으로도 도달 불가**다. 코퍼스에 연결이 없다.
서로 다른 문서의 단문 질문을 합성해 만든 데이터라 그렇게 설계된 것이다.

검색 측 기법으로 만들어낼 수 없는 성질이라면, MuSiQue를 계속 쫓는 대신
**적용 조건을 명확히 하고 해당하는 코퍼스에서 강점을 보이는 쪽**이 정직하다.
이미 그 조건을 재는 도구(`job_whyedge.py`)가 있다.

## C. 하지 말 것

- **헤비안 재시도.** 이득 상한이 구조적으로 0이다. 신호를 정답 피드백으로 바꾸지 않는 한
  파라미터를 어떻게 만져도 안 된다.
- **명제·술어 타입 재시도.** 문단 단위는 기각됐고, 문장 단위로 가려면 논항 공유가
  필요한데 그건 lift가 1/20이다. 두 경로가 다 막혔다.
- **논항 공유 엣지.** 사전 측정에서 정답은 덜 잇고 잡음은 10~20배 더 이었다.
- **ACT-R 합산.** 정답/잡음 수렴도가 같아 변별력이 없다.

이것들을 다시 하려면 **새로운 근거**가 필요하다. 파라미터를 바꿔보는 건 근거가 아니다.

## D. 프로젝트 밖 — 다음 연구 주제로 삼을 만한 것

지금 결과에서 가장 일반적인 관찰은 이것이다.

> 그래프 검색에서 **의미 구조를 더하는 방식은 반복적으로 실패하고,
> 희소화·억제 계열은 통한다.**

이걸 하나의 주장으로 세우려면 지금 있는 음성 결과 일곱 건에 더해
**다른 그래프 RAG 구현에서도 같은 패턴이 나오는지** 봐야 한다.
HippoRAG의 PPR에 희소 seed를 강제하면 어떻게 되는지가 가장 직접적인 확인이다.
그쪽 구현을 이미 우리 조건에서 돌릴 수 있으므로 추가 비용이 크지 않다.
