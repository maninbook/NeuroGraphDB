# 전략 (2026-08-03)

관련 연구 재점검·지연 실측·결과 감사를 마치고 다시 세운다.
근거는 전부 [RESULTS.md](RESULTS.md)에 있고, 여기는 **무엇을 하고 무엇을 그만둘지**만 적는다.

## 1. 상황 판단 — 우리가 어디에 있나

### 정확도 경쟁은 졌고, 앞으로도 진다

- MTEB 검색은 15개 평균이고 bge-base(우리 임베더)가 ~53, 상위권이 ~67이다. **출발점이 14점 뒤.**
- 임베더를 키우면 "110M으로 이만큼"이라는 주장 자체가 사라진다.
- 그래프에 의미 구조를 **더하는 시도가 8번 모두 실패**했다. 튜닝 문제가 아니라
  `all@10`이 칸 10개짜리 제로섬이라는 구조 문제다.
- **무게중심이 검색기에서 에이전트로 옮겨갔다.** 2026년 프런티어는 강화학습으로
  훈련된 검색 정책(AutoSearch·Agentic-R·Search-P1·CuSearch·TreePS-RAG)이고,
  Search-R1이 RAG 대비 상대 24%, Kimi-Researcher가 HLE 8.6→26.9%를 정책 훈련만으로 냈다.

**결론: 정확도로 순위표에 들어가려는 시도는 여기서 끝낸다.**

### 그런데 아무도 재지 않는 축에서 우리가 유일하다

| | 색인 LLM | 질의 LLM | 질의당 그래프 비용 |
|---|---|---|---|
| HippoRAG 2 | 문단당 2회 | 1회 | — |
| EcphoryRAG | 경량 개체추출 | ? | — |
| LogicRAG | **0회** | **여러 회** | — |
| **우리** | **0회** | **0회** | **0.01ms** |

- 검색 전체가 8.54ms인데 **그래프 확산은 0.01ms(0.1%)**다. 비용은 임베딩이고 그래프는 공짜다.
- **"그래프 RAG는 비싸다"는 통념이 우리에게는 성립하지 않는다.** 오늘 처음 숫자로 확인했다.
- 위키피디아 전체 520만 문단을 **24분**에 색인한다(연결률 59.0% 유지).
- 그리고 **에이전트는 한 질문에 검색을 5~20회 부른다.** 질의당 LLM 토큰이 곱해지는데
  **우리만 0으로 유지된다.**

### 리더 쪽에 우리가 이미 가진 것

- **3B + CoT + 자기일관성이 72B 평문을 2Wiki에서 넘는다**(0.566 vs 0.532).
- 4비트로 내려도 레시피가 살아남는다(1.8GB).
- 지렛대 순서: **검색 유무(+0.35) ≫ 리더 프롬프트(+0.14) > 검색 방법 차이(+0.03)**.

## 2. 재포지셔닝

> **"더 나은 그래프 검색기"에서 → "가장 싼 멀티홉 RAG 스택 전체"로.**

주장을 이렇게 바꾼다:

> 색인에도 질의에도 LLM을 쓰지 않는 검색기(질의당 0토큰, 그래프 0.01ms)와
> 3B 리더(CoT+자기일관성, 4비트 1.8GB)를 합치면, **6GB 안에서 70B급 멀티홉 QA**가 돈다.
> 위키피디아 전체를 24분에 색인하고, 에이전트가 몇 번을 질의하든 검색 토큰은 0이다.

이건 정확도 주장이 아니라 **비용-품질 곡선의 극단점 주장**이다. 순위표에 칸이 없을 뿐,
에이전트 시대에는 이 축이 더 중요해진다.

## 3. 할 일 — 우선순위 순

### T1. 재현성 복구 + 진단 도구 단독 배포 (최우선, 이미 착수)

감사에서 드러났듯 여러 수치가 **공개물만으로는 확인이 안 된다**(덮어쓰기).
이걸 못 고치면 위 주장 전부가 "로그에 있었다"로 끝난다.

- 잃어버린 산출물 재실행 (전부 값싼 CPU/L4 잡)
- `ngdb/` 패키지로 공통부 추출 + 기록값 고정 회귀 테스트
- **`diagnose.py` 단독 배포** — 임의 코퍼스에 대해 근거쌍 연결률·lift·홉 도달률·
  재배열 상한을 9초에 낸다. **남에게 가장 쓸모 있는 산출물이고 우리 정체성과도 맞는다.**
  "그래프 RAG가 내 코퍼스에서 통할까"에 답하는 도구는 아직 없다.

### T2. 에이전트 루프에서의 총비용 측정 (새 주력)

지금까지 잰 건 질의 1회 비용이다. **에이전트는 k회 부른다.** 그 축에서 비교표를 만든다.

- 간단한 반복검색 에이전트를 구현(질의 재작성 → 검색 → 판단 → 반복).
  **정책 훈련은 하지 않는다.** 우리 주장은 정책이 아니라 검색 단가다.
- 조건: 우리 검색기 / HippoRAG식(질의당 LLM 1회) / LogicRAG식(질의당 분해 LLM).
  **답변 LLM은 셋 다 동일하게 고정**해서 검색 비용만 남긴다.
- 낼 지표: 질문 1건당 **총 LLM 토큰**, 총 지연, 그리고 답변 정확도.
- **사전등록 필수.** 주 가설은 정확도가 아니라 **"동일 정확도에서 총 토큰이 낮다"**이다.
  정확도가 유의하게 낮으면 그대로 적는다.

**미리 정한 실패 조건**: 우리 검색기로 에이전트 정확도가 유의하게 떨어지면
"싸지만 못 쓴다"이므로 실패로 기록하고 T2를 접는다.

### T3. 스택 전체의 비용·품질·근거성 마무리

- 전체 스택(3B 4비트 + 우리 검색) 종단 메모리·지연 실측. 지금은 계산값뿐이다.
- **근거성 측정** — HHEM-2.1-Open(apache-2.0, flan-t5-base)으로 조건별
  "우리가 준 문맥이 답을 뒷받침하는가"를 채점. **검색 방식이 근거성에 미치는 영향**은
  기존 할루시네이션 리더보드가 안 다루는 축이다(전부 문맥을 고정하고 생성기만 잰다).
  다만 **우리에게 유리하리란 보장이 없다** — 오늘 반복해서 본 대로 검색 개선이
  종단 지표로 안 가는 경우가 많았다.

### T4. 프리프린트

T1~T3가 끝나면 쓴다. 리더보드가 아니라 arXiv + HF.
싣는 것: 비용 곡선의 극단점 주장, 사전등록 12건과 **음성 결과 8건**,
HippoRAG 동일조건 직접 비교, 전체 위키 규모 시연, 진단 도구.

**음성 결과가 이 논문의 절반이다.** 특히:
- 헤비안 공동인출 강화는 **이득 상한이 구조적으로 0**이다.
- **멀티홉에서 크로스인코더 재순위는 해롭다**(2Wiki 0.907 → 0.467). 기본 조언인데 틀렸다.
- 고정 예산에서 **찾는 문제와 앉히는 문제는 다르고**, 측정된 여지(최대 +30.6%p)와
  실현된 이득(0 또는 음수)의 격차가 그 비용이다.

## 4. 그만둘 것

- **정확도 리더보드.** 근거는 NEXT.md에 적어뒀다. 재론하지 않는다.
- **MuSiQue 추격.** 근거쌍의 63%가 어떤 홉으로도 도달 불가다. 코퍼스에 연결이 없고
  검색 기법으로 만들어낼 수 없다.
- **그래프에 의미 구조 더하기.** 8번 실패했다. 새 근거 없이 재시도하지 않는다.
- **개체 수준 링크를 LLM 없이 만들기.** EcphoryRAG가 **LLM으로** 그걸 해서 SOTA를 냈고,
  우리는 논항 공유로 시도했다가 lift가 952 → 78로 무너졌다.
  **이 경로는 우리 제약(LLM 0)과 양립하지 않는다.** 제약을 풀면 차별점이 사라지므로 접는다.

## 5. 정직하게 남겨둘 위험

- **2Wiki 우위가 그래프 덕분이라는 게 유일한 설명이 아니다.** LogicRAG이 그래프 없이
  2Wiki 64.7(HippoRAG 2는 50.0)을 냈다. 2Wiki가 템플릿 생성이라 질문 분해만 잘해도
  풀린다는 쪽이 더 단순한 설명일 수 있다.
- **비용 우위가 정확도 열위를 정당화하는지는 독자가 판단할 문제다.** 우리가
  "그래서 우리가 낫다"로 결론짓지 않는다. 곡선 위의 한 점을 정확히 제시하는 데서 멈춘다.
- **T2의 에이전트는 우리가 만든 것이다.** 자작 하네스에서 자기 방법이 유리하게 나오는 건
  흔한 함정이다. 에이전트 로직을 세 조건에 **완전히 동일**하게 두고, 코드를 공개한다.
