# 명제·기호학 표현층 — 설계와 사전등록

김기인의 원래 가설 중 **두 번째 절반**이다. 첫 절반(해마식 시냅스 강화)은
H1에서 측정 후 기각됐다(이득 상한이 구조적으로 0). 이쪽은 상황이 다르다.

> 헤비안이 실패한 이유는 **새 엣지가 인출 이력에서 나왔기** 때문이다.
> 이미 인출된 것끼리만 이어지니 발견이 불가능했다.
> 명제층의 엣지는 **텍스트에서** 나온다. 인출 이력과 무관하므로 이득 상한이 0이 아니다.

이건 낙관이 아니라 구조의 차이다. 그렇다고 성공이 보장되는 것도 아니다.

## 1. 지금 우리 엣지가 뭘 못 하는가

현재: `A의 본문에 B의 제목 문자열이 있으면 A→B`, 가중치 1.0 고정.

네 가지가 빠져 있다.

| 결함 | 구체적 증상 |
|---|---|
| **무타입** | "누가 X를 감독했나"와 "X는 어디서 태어났나"가 **같은 엣지**를 탄다 |
| **무극성** | "A는 B가 아니다", "A는 예전에 B였다"도 전부 긍정 링크가 된다 |
| **무조합제약** | 2홉 경로에 제한이 없어 depth≥2에서 도달 범위가 폭발한다 |
| **균일 가중** | 문장의 주어로 등장하든 부가절에 묻혀 있든 1.0으로 같다 |

세 번째가 관측된 약점과 맞는다. **MuSiQue의 손실이 유독 많았다**(139/55). MuSiQue는
2~4홉이고 조합이 어렵다. 무제약 확산은 깊이가 깊어질수록 정밀도를 잃는다.
이건 사후 해석이 아니라 **이미 기록된 미진단 항목**이다.

## 2. 설계

각 언급을 **명제 사례**로 표현한다.

```
(주어, 술어, 목적어) + 극성 + 시간한정
    "Nolan directed Inception"        → (Nolan, direct, Inception, +)
    "Inception was not written by X"  → (X, write, Inception, −)
    "formerly known as Y"             → (·, alias, Y, +, 과거)
```

엣지가 **방향 + 타입**을 갖게 된다: `A --[direct]--> B`.
방향은 이미 있다(`add_edge`는 단방향, `A가 B를 언급`). **타입만 얹으면 된다.**

질의 시점에는 질문을 **관계 골격**으로 파싱한다.

```
"Who directed the film that X starred in?"
    → ?p --[direct]--> ?f  ∧  X --[star_in]--> ?f
```

그리고 **골격과 맞지 않는 술어는 확산에서 감쇠**시킨다.

### LLM을 쓰지 않는다 — 이게 협상 불가 조건이다

우리의 현재 우위는 "엣지 생성 LLM 비용 0"이다. 명제 추출에 문단마다 LLM을 쓰면
**우리는 그냥 HippoRAG가 된다.** 그러면 이 연구를 할 이유가 없다.

- 의존 구문 분석 spaCy `en_core_web_sm` (CPU, 문단당 수 ms)
- 술어 = 지배 동사의 표제어 + 전치사 (`direct`, `bear_in`, `locate_in`)
- 극성 = `neg` 의존 관계 존재 여부
- 술어 타입 축약 = 술어 문자열을 bge로 임베딩해 군집화 — **이미 로드된 모델**을 쓴다

전부 결정적이고 재현 가능하며 API 호출이 없다.

### 하드 프루닝이 아니라 소프트 가중

술어 매칭은 취약하다. 맞으면 통과/틀리면 차단으로 하면 파싱 실패 한 번이 정답을 죽인다.

```
w_effective = w_base × (1.0 if 술어 호환 else α)      α = 0.3
극성이 음(−)이면                                       × β,  β = 0.2
```

α, β는 **실행 전에 고정한다.** 돌려보고 고르면 그건 튜닝이지 검증이 아니다.

## 3. 사전등록 — 돌리기 전에 적는다

앞서 두 번(BitBudget 대응설계, slowcode 1문항 승리 선언) 사후 판단으로 틀렸다.
같은 실수를 세 번째로 하지 않기 위해 성공 조건을 여기 먼저 박아둔다.

**주 가설**
MuSiQue 근거2개@10에서 `우리그래프+명제층`이 `우리그래프`를 앞선다.
검정: McNemar 대응 이분, α=0.05, n=1000. 다른 검정으로 바꾸지 않는다.

**부 가설**
HotpotQA·2Wiki 동일 지표. 유의하지 않아도 주 가설과 무관하게 보고한다.

**안전장치 (이걸 어기면 실패로 본다)**
HotpotQA R@10이 1.0%p 넘게 떨어지면, 주 가설이 통과해도 **실패**로 기록한다.
한 데이터셋을 위해 다른 데이터셋을 망가뜨리는 건 개선이 아니다.

**기전 예측 (맞아야 설명이 성립한다)**
이득은 **정답 문단이 dense 상위 20위 밖에 있던 질문**에 몰려야 한다.
거기가 그래프가 실제로 일하는 자리다. 이득이 다른 데 흩어져 있으면
기전 설명이 틀린 것이고, 숫자가 좋아도 그렇게 적는다.

**절제 실험 (셋을 따로 돌린다 — 뭉치면 뭐가 일했는지 모른다)**
- P1 술어 타입만
- P2 극성만
- P3 둘 다

**미리 정한 실패 조건**
P3가 유의하지 않으면 "명제 표현은 표준 멀티홉 검색에서 이득을 못 냈다"로 적고,
파라미터를 바꿔가며 재시도하지 않는다. α·β 재탐색은 **새 사전등록**으로만 한다.

## 4. 예상되는 실패 모드

정직하게 적어둔다. 나중에 변명하지 않기 위해서다.

1. **spaCy 파싱 품질이 위키 문체에서 낮을 수 있다.** 긴 동격구와 괄호가 많다.
2. **술어 군집이 너무 거칠면** 호환 판정이 사실상 항상 참이 되어 통제군과 같아진다.
3. **질문 파싱이 더 어렵다.** 질문은 술어가 명시되지만 목적어가 변수라 골격 추출이 흔들린다.
4. **HotpotQA는 이미 0.94다.** 천장이 가까워 개선 여지가 거의 없다.
   주 가설을 MuSiQue로 잡은 이유가 이것이다 — 여유가 거기 있다.

## 5. 순서

1. spaCy 명제 추출기 (C++ 코어는 그대로, 파이썬에서 전처리 → 타입/극성을 엣지에 실어 넘김)
2. `Graph`에 엣지 타입 필드 추가 (C++ — `adj_`의 값이 float에서 {w, type, polarity}로)
3. 질문 골격 파서
4. P1/P2/P3 절제 실험을 세 데이터셋에 실행

2번이 C++ 변경이라 기존 실험과의 호환을 깨지 않도록 타입 없는 엣지는
"모든 술어와 호환"으로 동작하게 둔다. 그러면 통제군이 **정확히** 현재 코드와 같아진다.
