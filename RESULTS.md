# NeuroGraphDB 실험 기록

## B0 — baseline 확정 (2026-08-01)

HotpotQA distractor, 질문 1000 / 문단 풀 9,798. 색인·검색은 전부 C++(ngdb).
**논문 공개 수치가 아니라 직접 돌린 값이다.**

| 방법 | R@1 | R@2 | R@5 | R@10 | R@20 | 근거2개@10 |
|---|---|---|---|---|---|---|
| BM25 | 0.384 | 0.547 | 0.718 | 0.844 | 0.903 | 0.694 |
| 조밀 벡터 (bge-base) | 0.455 | 0.738 | 0.869 | 0.927 | 0.956 | **0.857** |
| 하이브리드 (RRF) | 0.423 | 0.652 | 0.841 | 0.932 | 0.959 | 0.864 |

- 하이브리드가 벡터 단독보다 거의 안 낫다 (0.864 vs 0.857). RRF가 BM25의 약한 순위에 끌린다
- BM25만으로도 69.4%가 풀린다. 어휘 겹침의 힘이 생각보다 크다

## G1 — 희소 seed + 제목 언급 그래프 (2026-08-01)

**진단이 맞았다.** NeuroDB는 전부를 seed로 잡아 그래프가 일할 자리가 없었다.
seed를 희소하게 잡으면 그래프 채널이 살아난다.

엣지는 LLM 비용 0 — **문단 본문이 다른 문단의 제목을 언급하면 엣지.**
검색은 상위 n개 dense 히트를 seed로 확산(depth 3, decay 0.65) 후 dense 순위로 채운다.

| 데이터셋 | 풀 | 엣지(노드당) | dense | graph@5 | 개선 | McNemar 이득/손실 |
|---|---|---|---|---|---|---|
| HotpotQA | 9,798 | 0.6 | 0.857 | **0.940** | +8.3%p | 89 / 6 |
| 2Wiki | 6,250 | 0.4 | 0.486 | **0.907** | +42.1%p | 426 / 5 |
| MuSiQue | 9,897 | 1.1 | 0.266 | **0.350** | +8.4%p | 139 / 55 |

셋 다 p<0.0001. 사전 지정한 seeds=5에서 판정했다(최적값을 사후에 고르지 않았다).

### seed 희소성이 열쇠라는 증거

세 데이터셋 모두 **seed가 10개를 넘는 순간 dense와 같아진다.**

| seeds | HotpotQA | 2Wiki | MuSiQue |
|---|---|---|---|
| 1 | 0.942 | 0.719 | 0.286 |
| 5 | 0.940 | 0.907 | 0.350 |
| 10 | 0.860 | 0.487 | 0.269 |
| 200 | 0.860 | 0.487 | 0.269 |
| (dense) | 0.857 | 0.486 | 0.266 |

seed 200은 NeuroDB v0.1의 상태를 재현한 조건이다. 정확히 dense로 무너진다.
**패턴 분리(희소 seed)가 없으면 패턴 완성(그래프 확산)도 일어나지 않는다.**

### 정직하게 볼 것

1. **MuSiQue에서 손실 55건.** HotpotQA(6)·2Wiki(5)와 확연히 다르다. 가장 어려운
   데이터셋에서 그래프가 55문제를 망친다. 잘못된 엣지를 타는 것으로 보이며 진단이 필요하다
2. **2Wiki의 +42%p는 과대평가일 수 있다.** 2Wiki는 Wikidata 관계에서 템플릿 생성된
   질문이라 구조 이용 방법에 유난히 유리하다. dense가 0.486으로 낮은 것도 같은 이유
3. **HippoRAG을 이긴 게 아니다.** 우리는 **검색 재현율**, 그쪽 공개 수치는 **답변 정확도**다.
   (HippoRAG2: HotpotQA 56.7 / 2Wiki 50.0 / MuSiQue 27.0 — 모두 str_acc)
   비교하려면 LLM을 붙여 종단 QA를 재야 한다
4. **graph는 dense 위의 재순위**다. 독립 검색기가 아니라 상위를 재배열한 것

### 확정된 주장

> 희소 seed + 제목 언급 그래프 확산이 조밀 벡터 검색을 **세 표준 멀티홉 벤치마크에서
> 유의하게 앞선다.** 엣지 생성에 LLM 비용이 들지 않고 그래프는 노드당 1개 미만으로 희소하다.

## 아직 시험하지 않은 것 — 사용자 가설의 핵심

**헤비안 강화(인출이 DB를 바꾼다)는 아직 켜지 않았다.**
1회성 QA 벤치마크에는 강화할 사용 이력이 없기 때문이다. 별도 평가 설계가 필요하다.
[NEUROGRAPHDB.md](NEUROGRAPHDB.md) 참조.

## Q1 — 종단 QA (2026-08-01)

같은 LLM(Qwen2.5-7B-Instruct)·같은 프롬프트·같은 top-k에서 **검색만 바꿨다.**
LLM을 잡 안에서 vLLM으로 서빙해 모델 버전을 고정했다(외부 API는 재현이 깨진다).

### top-5 — 차이 없음 (불일치 0)

`dense@5 = graph@5 = EM 0.482`. 두 조건이 **모든 문항에서 같은 답**을 냈다.

원인은 G1 표에 이미 있었다. R@1·R@2·R@5가 dense와 소수점까지 같고
**이득은 6~20위 구간에만 있다.** 확산에서 seed는 자기 dense 점수를 갖고
이웃은 `점수 × 가중치 × 0.65`를 받는데, top-5 dense 점수가 촘촘해서
(예: 0.90/0.85/0.83/0.82/0.81) 한 홉에 35%를 깎인 이웃이 5위를 넘지 못한다.
→ 상위 5개는 언제나 dense 그대로다.

### top-10 — 세 데이터셋 모두 유의하게 앞섬

| | dense EM | graph EM | dense F1 | graph F1 | McNemar 이득/손실 | p |
|---|---|---|---|---|---|---|
| HotpotQA | 0.514 | **0.539** | 0.648 | **0.680** | 41 / 16 | 0.0013 |
| 2Wiki | 0.390 | **0.453** | 0.452 | **0.542** | 91 / 28 | <0.0001 |
| MuSiQue | 0.144 | **0.168** | 0.236 | **0.255** | 50 / 26 | 0.0079 |

**검색 재현율 이득이 답변 정확도로 전이된다.** 2Wiki에서 가장 크고(+6.3%p EM,
+9.0%p F1), 검색에서도 가장 컸던 것과 일치한다.

### 적용 조건 (숨기지 않는다)

> 이 방법은 **6~20위 구간의 재현율**을 올린다. top-5만 쓰는 파이프라인에서는 이득이 없고,
> top-10 이상을 쓸 때 나타난다.

한계이자 적용 조건이다. 명시하지 않으면 재현 시도에서 바로 드러난다.

### 공개 수치와의 관계 — 비교 아님

참고로 병기하되 **직접 비교가 아니다.** LLM(그쪽은 70B급), 프롬프트, str_acc/EM 정의가 다르다.

| | HotpotQA | 2Wiki | MuSiQue |
|---|---|---|---|
| HippoRAG2 (str_acc, 공개) | 56.7 | 50.0 | 27.0 |
| LogicRAG (str_acc, 공개) | 54.8 | 64.7 | 30.4 |
| 우리 graph (EM, 7B) | 53.9 | 45.3 | 16.8 |

진짜 비교는 그쪽 공개 구현을 받아 **같은 LLM·같은 조건에서 우리가 직접 돌리는 것**이다.

### 우리가 정직하게 주장할 수 있는 것

> 동일 LLM·프롬프트·top-k에서, 희소 seed + 제목 언급 그래프 확산이
> 조밀 벡터 검색 대비 **세 표준 멀티홉 벤치마크 모두에서 QA 정확도를 유의하게 올린다**
> (EM +2.4~6.3%p, 전부 p<0.01). 엣지 생성에 LLM 비용이 들지 않는다.

### 환경 함정 (HF Jobs + uv) — 다음에 또 만난다

- `vllm`에 하한을 안 걸면 0.2.5(2023)로 후퇴 → 휠 없음 → 소스 빌드 → CUDA_HOME 없음
- `datasets` 5.x가 `torchcodec`을 끌어옴 → FFmpeg 공유 라이브러리 없음 → 죽음
- **`sentence-transformers`도 import 시점에 torchcodec을 끌어옴**(멀티모달)
- → 해법: `datasets`·`sentence-transformers` 둘 다 걷어내고
  parquet 직접 읽기(`refs/convert/parquet` 브랜치) + transformers로 직접 임베딩.
  bge는 CLS 풀링 + L2 정규화이며 sentence-transformers와 **최대 절대차 0.000000** 확인

## H1 — 헤비안 강화 (2026-08-01) · **기각**

프로토콜: 질문 2000개를 준비 1000 / 측정 1000으로 가르고, 준비 단계에서만
공동인출 쌍을 강화한 뒤 **같은 측정 질문**을 두 조건이 푼다(대응 설계).
강화 신호는 공동인출만 — 정답 라벨을 보고 강화하면 라벨 누수다.

HotpotQA, delta=0.05, 강화상위 8, 풀 19,296

| 조건 | R@10 | 근거2개@10 |
|---|---|---|
| 통제군 | 0.939 | 0.888 |
| 헤비안 | 0.928 | **0.868** |

**McNemar 이득 0 / 손실 20, p<0.0001 → 헤비안이 유의하게 나쁘다.**

부익부 점검: 꼬리(준비 단계 미노출) 0.878 → 0.859. 자주 쓰인 것이 세지며 드문 주제를 민다.

### 이득 0은 우연이 아니라 구조다

1000문항에서 이긴 사례가 **하나도 없다.** 원인은 신호의 정의에 있다.

> 공동인출로 강화하면 **엣지는 이미 인출된 문단끼리만 생긴다.**
> 나중 질의가 그 이득을 보려면 **같은 쌍이 다시 필요해야** 한다.
> 무작위 표집된 독립 질문에는 그 반복 구조가 없다.
> 따라서 이득의 기댓값 상한이 0이고, 비용(밀어내기)만 남는다.

밀어내는 경로도 확인됐다. 엣지 18,530 → 65,791 (3.5배). delta=0.05·상한 1.0이라
**20번 함께 뜬 쌍은 진짜 언급 엣지와 동일한 가중치 1.0**이 되어 정답을 10위 밖으로 민다.

파라미터 튜닝 문제가 아니다. delta를 줄이면 통제군에 수렴할 뿐, 이득 쪽으로 가지 않는다.

### 남길 문장

공동인출 기반 헤비안 강화는 표준 멀티홉 검색에서 **이득의 상한이 0**이다.
강화 대상이 정의상 이미 인출된 문단이기 때문이다. 측정 결과 이득 0 / 손실 20
(p<0.0001), 드문 주제에서 손해가 크다. 이 방향이 의미를 가지려면 **반복 사용 구조**가
있는 작업부하가 필요하며, 그건 벤치마크를 자작한다는 비판을 받는다.

GraphRAG 논문들이 적응형 강화를 "향후 과제"로 자주 적지만 실제로 재고 조건까지
특정한 결과는 드물다. 음성 결과지만 그래서 쓸모가 있다.

## P — 명제·기호학 표현층 (2026-08-01) · **기각**

사전등록: ../PROPOSITION.md (돌리기 전에 작성). alpha=0.3, beta=0.2 고정, n=1000.
엣지 집합은 통제군과 **동일**하고 술어 타입·극성만 얹었다. 술어 추출은 spaCy만 쓴다(LLM 0).

### 통제군 재현 확인

| | dense 단독 | P0 통제군 | G1 기록 |
|---|---|---|---|
| HotpotQA | 0.857 | 0.940 | 0.857 / 0.940 — **정확히 일치** |
| 2Wiki | 0.486 | 0.907 | 0.486 / 0.907 — **정확히 일치** |
| MuSiQue | 0.259 | 0.341 | 0.266 / 0.350 — 0.007 낮음 |

MuSiQue의 어긋남은 **dense에서 이미 발생**한다(0.259 vs 0.266). 그래프가 얹는 이득은
양쪽 모두 +0.082~0.084로 같다. 잡 간 임베딩 수치 차이(라이브러리 버전·GPU 비결정성)이며
네 조건이 같은 임베딩을 공유하므로 절제 비교에는 영향이 없다.

### 결과 — 주 가설 기각

MuSiQue 근거2개@10:

| 조건 | R@10 | 근거2개@10 | vs 통제군 |
|---|---|---|---|
| P0 통제군 | 0.640 | 0.341 | — |
| P1 타입만 | 0.615 | 0.311 | 이득 6 / 손실 36, p<0.0001 → **뒤짐** |
| P2 극성만 | 0.639 | 0.340 | 이득 0 / 손실 1 → 차이 없음 |
| P3 둘 다 | 0.614 | 0.309 | 이득 6 / 손실 38, p<0.0001 → **뒤짐** |

HotpotQA는 네 조건이 **완전히 동일**(불일치 0). 2Wiki도 사실상 동일(손실 2).

**안전장치 위반**: MuSiQue R@10이 2.6%p 하락(한계 1.0%p). 사전등록대로 실패로 기록한다.

### 왜 실패했나 — 기전이 명확하다

**1. 계사가 술어를 잡아먹는다.** 타입 부착된 엣지 중 `be`가 HotpotQA 4,493/5,823,
2Wiki 1,747/2,547, MuSiQue 7,015/11,129. 위키 도입부는 "X is a Y" 문장이라
지배 동사가 대부분 계사다. **관계 정보를 거의 담지 않는다.**

**2. 질문의 동사와 본문의 지배 동사가 다르다.** "Who is the sibling of the director of X?"
의 동사는 `be`뿐인데, 정답 경로의 엣지는 `direct`로 타입이 붙어 있다.
그래서 제약이 **맞는 경로를 눌러버린다.**

**3. 감쇠가 홉마다 곱해진다.** alpha=0.3이 2홉이면 0.09다. 그래서 손해가
**깊은 탐색이 필요한 곳에 정확히 몰린다.** 기전 확인이 이걸 그대로 보여준다:

| MuSiQue 층 | n | 이득 | 손실 |
|---|---|---|---|
| dense가 이미 담음 | 363 | 4 | 7 |
| **dense 밖 (그래프가 일할 자리)** | **637** | **2** | **31** |

HotpotQA·2Wiki에서 효과가 0인 것도 같은 이유다. 그쪽은 대부분 1홉이라 감쇠가
순위를 뒤집을 만큼 누적되지 않는다.

**4. 부정은 거의 없다.** 부정 극성 엣지가 HotpotQA 10개, 2Wiki 4개, MuSiQue 50개.
P2가 무효과인 건 당연하다 — 위키 도입부에 부정문이 없다.

### 남길 문장

동사 표제어로 술어 타입을 만들어 확산을 제약하는 방식은 표준 멀티홉 검색에서
**이득이 없거나 해롭다.** 위키 문체의 지배 동사가 계사에 쏠려 관계 정보를 담지 못하고,
질문의 동사와도 어긋나며, 곱셈 감쇠가 홉마다 누적돼 **다홉 경로에 가장 큰 손해**를 준다.
극성은 부정문 자체가 희소해 측정 불가다.

사전등록대로 alpha/beta를 바꿔가며 재시도하지 않는다. 재탐색은 새 사전등록으로만 한다.

## A — HippoRAG 2 정면 비교 (2026-08-01)

공개 수치와 나란히 놓는 게 아니라 **그쪽 구현을 우리 조건에서 직접 돌렸다.**
n=500, 풀·질문·질문순서·임베더가 모두 동일하다(코드에서 assert로 확인).

임베더를 맞추기 위해 `BaseEmbeddingModel` 인터페이스에 우리 bge 인코딩을 구현해 넣었다.
기본값 NV-Embed-v2(7B)를 그냥 두면 **임베더 차이(110M vs 7B)를 그래프 차이로 착각**한다.
OpenIE·PPR·사실 재순위 등 알고리즘은 건드리지 않았다.

### 검색 결과 — 근거 전부를 상위 10에 담은 비율

| | dense | **우리 graph** | HippoRAG2 |
|---|---|---|---|
| HotpotQA | 0.874 | **0.940** | 0.934 |
| 2Wiki | 0.534 | **0.920** | 0.796 |
| MuSiQue | 0.330 | 0.394 | **0.454** |

### R@k — 갈리는 지점이 뚜렷하다

| | R@5 | | R@10 | | R@20 | |
|---|---|---|---|---|---|---|
| | 우리 | Hippo | 우리 | Hippo | 우리 | Hippo |
| HotpotQA | 0.879 | **0.922** | **0.970** | 0.966 | **0.987** | 0.984 |
| 2Wiki | 0.734 | **0.887** | **0.970** | 0.923 | **0.978** | 0.935 |
| MuSiQue | 0.584 | **0.632** | 0.686 | **0.725** | 0.721 | **0.781** |

**HippoRAG은 상위 5위를 잘 만든다.** 사실 재순위(recognition memory)가 머리를 다듬는다.
**우리는 10~20위 구간이 강하다.** 앞서 top-5에서 이득이 0이었던 것과 같은 성질이다.
MuSiQue는 전 구간에서 진다 — 홉이 깊을수록 개체 그래프가 유리하다.

### 색인 비용이 다르다

| | HotpotQA 4,943문단 색인 |
|---|---|
| HippoRAG2 | **74분** (문단당 LLM 2회 — NER + 트리플 추출) |
| 우리 | **수 초** (제목 문자열 매칭, LLM 0회) |

HotpotQA 0.940 vs 0.934를 **색인 비용 수천 분의 1**로 낸다. 이게 우리 주장의 중심이다.

### 정직하게 붙일 것

HippoRAG을 Qwen2.5-7B로 돌렸다. 그쪽 논문은 70B급을 쓴다. 로그에 재순위 필터의
파싱 실패(`Error parsing field fact_after_filter`)가 반복해서 찍혔다 —
**작은 LLM이 그쪽 파이프라인을 깎는다.** 통제로는 동일 LLM이 옳지만 그들에게 불리하다.

동시에 이건 우리 논점이기도 하다. **우리 그래프는 LLM을 쓰지 않아 이 열화가 없다.**

## A3 — 3자 QA, 생성 LLM을 72B로 (2026-08-01)

Qwen2.5-**72B**-Instruct, top-10, n=500. 풀·질문·임베더·프롬프트 전부 동일, 검색만 다름.
Llama-3.3-70B(HippoRAG 논문 모델)은 gated 미승인이라 Qwen을 썼다.
7B 실행과 계열·토크나이저·채팅템플릿이 같아 **바뀐 변수가 크기 하나**다.

| | dense | 우리 graph | HippoRAG2 |
|---|---|---|---|
| **HotpotQA** EM/F1 | .598/.729 | **.612/.755** | .604/.740 |
| **2Wiki** EM/F1 | .460/.531 | **.532/.635** | .512/.613 |
| **MuSiQue** EM/F1 | .218/.324 | .238/.341 | **.272/.380** |

### McNemar — 여기가 중요하다

| | dense vs graph | graph vs HippoRAG |
|---|---|---|
| HotpotQA | 15/8, p=0.21 — 유의하지 않음 | 17/21, p=0.63 — 유의하지 않음 |
| 2Wiki | 41/5, **p<0.0001** | 20/30, p=0.20 — 유의하지 않음 |
| MuSiQue | 26/16, p=0.16 — 유의하지 않음 | 32/15, **p=0.019 → HippoRAG 우세** |

**QA에서 HippoRAG을 유의하게 이긴 데이터셋은 없다.** 2Wiki는 수치상 앞서지만(.532 vs .512)
p=0.20이다. MuSiQue는 그쪽이 유의하게 앞선다. 이걸 "우리가 이겼다"고 쓰면 안 된다.

### 큰 LLM이 검색 우위를 깎는다

7B·n=1000에서는 graph가 dense를 **세 데이터셋 모두** 유의하게 앞섰다
(p=0.0013 / 0.0000 / 0.0079). 72B·n=500에서는 **2Wiki만** 남았다.

두 가지가 섞여 있다 — n이 절반이라 검정력이 떨어졌고(이건 우리 탓),
**72B가 근거가 부족해도 답을 맞춘다.** 2Wiki에서 검색은 +12.4%p 앞서는데(0.920 vs 0.796)
QA 차이는 2%p로 줄어든 게 그 증거다. 검색을 개선해도 큰 모델이 그 이득을 흡수한다.

이건 우리 방법에 불리한 사실이고, 그대로 적는다.

## 왜 2Wiki에서 이기고 MuSiQue에서 지는가 — 검증됨

근거 문단 쌍이 우리 엣지로 **직접 연결**돼 있는 비율을 셌다(LLM·GPU 불필요, 9초).

| | 근거쌍 직접연결 | 노드당 엣지 | dense 출발점 | dense 대비 이득 |
|---|---|---|---|---|
| 2Wiki | **68.8%** | 0.35 | 0.534 | **+38.6%p** |
| HotpotQA | 61.0% | 0.51 | 0.874 | +6.6%p |
| MuSiQue | **32.4%** | 0.92 | 0.330 | +6.4%p |

> **이득 = (dense가 남긴 여지) × (근거쌍이 우리 엣지로 이어져 있을 확률)**

2Wiki는 둘 다 갖췄다. HotpotQA는 연결은 높은데 dense가 이미 0.874라 올릴 자리가 없다.
MuSiQue는 여지는 큰데 연결이 32.4%뿐이라 갈 길이 없다.

MuSiQue는 **노드당 엣지가 가장 많은데(0.92) 근거쌍 연결은 가장 낮다.**
엣지가 엉뚱한 것들을 잇는다. HippoRAG이 거기서 이기는 이유이기도 하다 —
개체 단위로 쪼개면 제목 참조가 없어도 경로가 생긴다.

### 적용 조건이 숫자로 특정됐다

2Wiki의 68.8%는 **데이터셋 제작 방식의 부산물**이다(Wikidata 관계 템플릿).
일반 문서 집합에서 그대로 나오지 않는다. 우리 방법의 조건은
**"문서들이 서로 제목으로 참조하는 코퍼스"**다. 위키·사내문서·논문은 해당하고,
합성된 다홉 과제는 해당하지 않는다.

쓸모 있는 부산물: **색인 전에 근거쌍 연결률만 재면** 이 방법이 그 코퍼스에서
먹힐지 미리 안다. 9초짜리 검사다.

### [정정 2026-08-01] P 실험의 범위를 잘못 적었다

김기인의 원래 가설은 **문장 단위 명제**다 — 문장 안에 개념과 관계가 있고 그것들이
논리·기호학으로 연결된다. 그런데 구현은 **문단 단위**로 했다.

| | 김기인이 말한 것 | 내가 만든 것 |
|---|---|---|
| 단위 | 문장에서 뽑은 명제 | 문단 |
| 노드 | 명제 (주어·술어·목적어) | 문단 |
| 엣지 | 명제 간 논리 관계 | 문단이 문단 제목을 언급 |
| 명제의 지위 | 일급 객체 | 엣지에 붙인 라벨 하나 |

게다가 `propositions.py`에서 문단 쌍마다 **첫 언급의 술어 하나만 남기고 버렸다**
(`cands[0]`). 한 문단이 같은 대상을 여러 관계로 언급해도 하나로 붕괴시킨 것이다.
`be`가 술어의 대부분을 차지한 원인 중 하나가 이 설계다 —
문장별로 뽑았다면 "X는 영화다"와 "놀란이 감독했다"가 별개 명제로 남는다.

**따라서 P 실험은 원래 가설을 검정하지 않았다.** 기각된 것은
"문단 쌍 엣지에 지배동사 라벨을 하나 붙여 확산을 제약하는 방식"이지
"문장 단위 명제 표현"이 아니다. 앞의 "남길 문장"은 그 범위로만 읽어야 한다.

## 사전 측정 — 문장 단위 명제로 가기 전에 (2026-08-01)

정정 이후, 김기인의 원래 가설(문장 단위 명제)을 제대로 검정하려면
명제의 **논항 공유**로 문단을 잇는 게 핵심이다. 제목 언급이 없어도 이어지기 때문이다.
실험을 짜기 전에 **그럴 가치가 있는지**부터 쟀다. 연결률만 보면 안 되고,
아무거나 다 이으면 무의미하므로 무작위쌍 연결률(=밀도)과 lift를 같이 쟀다.

### 1) 논항 공유는 두 축 모두에서 더 나쁘다

| 연결 정의 | 정답쌍 (Hot/2W/MuS) | 무작위쌍 | lift |
|---|---|---|---|
| **제목 언급 (현재)** | 61.0 / 68.8 / 32.4% | 0.017 / 0.016 / 0.034% | **3587 / 4437 / 952** |
| 논항 all | 24.8 / 23.8 / 25.4% | 1.470 / 1.346 / 1.165% | 17 / 18 / 22 |
| 논항 propn | 16.6 / 16.6 / 22.1% | 0.532 / 0.226 / 0.371% | 31 / 74 / 60 |
| 논항 propn6 | 15.2 / 14.2 / 20.5% | 0.343 / 0.158 / 0.261% | 44 / 90 / 78 |

MuSiQue조차 20.5% < 32.4%다. **정답은 덜 잇고 잡음은 10~20배 더 잇는다.**

이유: **제목 언급은 이미 개체 링크이고, 그것도 매우 좋은 것이다.**
제목은 개체의 정식 이름이고, 그걸 언급한다는 건 **그 개체를 설명하는 문서를
정확히 가리키는 것**이다. 논항 공유는 같은 문자열이 나온 문단을 전부 잇는다
("United States"를 공유하는 수백 개 문단).

### 2) 홉을 늘려도 안 닿는다 — 진단이 뒤집혔다

논항 공유가 노리던 A–C–B 경로는 우리 확산(3홉)에 이미 있을 수 있다.
그러면 "경로가 없다"가 아니라 "순위가 안 오른다"가 된다. 그래서 홉별 도달률을 쟀다.

| 근거쌍 도달률 | 1홉 | 2홉 | 3홉 | 무작위쌍 3홉 |
|---|---|---|---|---|
| HotpotQA | 61.0% | 61.6% | 61.6% | 0.0% |
| 2Wiki | 68.8% | 69.0% | 69.0% | 0.0% |
| MuSiQue | 32.4% | 36.7% | **37.1%** | 0.2% |

**경로가 정말로 없다.** 무작위쌍이 3홉에서도 0%라는 건 그래프가 거의 연결돼 있지
않다는 뜻이다 — 노드당 엣지 0.35~0.92개, 대부분 노드는 나가는 엣지가 없다.

### MuSiQue 패배의 진짜 원인

**닿을 수 있는 근거쌍이 37.1%인데 dense가 이미 33.0%를 가져온다.**
그래프가 완벽해도 여지가 4%p 남짓이고, 실제 이득이 +6.4%p였으니 이미 천장이다.
감쇠 조정이나 명제 표현으로 될 문제가 아니었다. **연결성 자체가 없다.**

### 부수적으로 — max_depth=3은 사실상 장식이다

1홉과 3홉 차이가 0.6 / 0.2 / 4.7%p다. 이득이 거의 전부 1홉에서 나온다.
`max_depth=1`로 두어도 결과가 거의 같을 것이다. 확인해서 맞으면 알고리즘을 줄인다.

### 결론

문장 단위 명제로 옮겨도 **연결성 문제를 못 푼다.** 논항 공유의 lift가 제목 언급의
1/20~1/50이라 엣지를 늘리는 방법으로 쓸 수 없다. 이 방향은 여기서 멈춘다.

이건 "구조를 더하면 안 된다"는 뜻이 아니라 **"우리 병목은 의미 표현이 아니라
연결성"**이라는 뜻이다. 연결을 정밀하게 늘리는 방법을 찾으면 그때 의미 표현이 의미를 갖는다.

## 안 이어지는 근거쌍의 원인 규명 (2026-08-01)

닿지 않는 근거쌍(39/31/68%)을 자동 분류하고 실물을 읽었다.

### 가장 큰 덩어리는 우리 버그였다

위키 제목의 **동음이의 괄호**를 `norm()`이 공백으로 바꿔서
`'Test (wrestler)'` → `"test wrestler"`가 되는데, 본문에는 `"Test"`로만 나온다.
**개체가 본문에 멀쩡히 있는데 우리가 못 찾고 있었다.**

    'Test (wrestler)'    ← 본문 "Lance Storm, Christian and Test"
    'The Shining (film)' ← 본문 'a sample from the film "The Shining"'
    'Sicyon (mythology)' ← 본문 "the ancient city Sicyon"

`'Maryland gubernatorial election, 2018'` 같은 쉼표 접미사도 같은 유형.
괄호 달린 제목이 풀의 17.7 / 14.9 / 10.5%다.

### 고칠 수 없는 몫도 크다

"어휘 겹침 없음"이 34.9 / 53.2 / 28.2%. 대부분 **비교형 질문**이다
(`'Michael Casey (poet)'` vs `'Matt Vasgersian'` — 시인과 스포츠 캐스터).
두 문단 사이에 관계가 애초에 없다. **그래프가 도울 일이 아니고 dense가 둘 다 찾아야 한다.**

### 별칭 키 추가 — 연결은 늘고 잡음도 는다

| | 정답쌍 연결 | lift | 엣지/노드 |
|---|---|---|---|
| HotpotQA 현재→별칭 | 61.0→**69.2%** | 3587→1628 | 0.51→1.13 |
| 2Wiki | 68.8→**73.6%** | 4437→1599 | 0.35→1.01 |
| MuSiQue | 32.4→**35.2%** | 952→774 | 0.92→774 |

기각한 논항 공유(lift 44~90)와는 자릿수가 다르다.

## E — 제목 별칭 검색 평가 · **주 가설 기각**

사전등록: 주 가설 HotpotQA 근거2개@10, 안전장치 R@10 −1.0%p, 기전은 새로 연결된 질문에 이득 집중.

| | 근거2개@10 | McNemar | 판정 |
|---|---|---|---|
| **HotpotQA (주)** | 0.940 → **0.921** | 11/30, p=0.0043 | **뒤짐**, 안전장치 위반(−1.1%p) |
| 2Wiki | 0.907 → **0.947** | 63/23, p<0.0001 | 앞섬 |
| MuSiQue | 0.341 → 0.344 | 14/11, p=0.69 | 차이 없음 |

**주 가설 기각.** 2Wiki가 좋아졌다고 그걸 성과로 바꿔 쓰지 않는다.

### 기전 예측은 셋 다 맞았다

| | 새로 연결된 질문 | 그 외 |
|---|---|---|
| HotpotQA | 이득 9 / 손실 2 (n=79) | 이득 2 / **손실 28** |
| 2Wiki | 이득 35 / **손실 0** (n=56) | 이득 28 / 손실 23 |
| MuSiQue | 이득 9 / **손실 0** (n=35) | 이득 5 / 손실 11 |

새로 연결된 질문에서 **이득 53 / 손실 2.** 별칭 엣지는 의도한 자리에서 의도대로 작동한다.
문제는 **나머지 질문의 희석 손해**다.

### 규칙성 — dense가 강한 곳에서 손해가 크다

HotpotQA는 dense가 이미 0.857이라 올릴 자리는 없고 밀려날 정답만 많다.
2Wiki는 dense가 0.486이라 여유가 크다. 같은 엣지가 정반대로 작용한다.

### 다음에 할 수 있는 것 (아직 안 함)

별칭 엣지는 정확 제목 엣지보다 **정밀도가 낮은데 가중치가 똑같이 1.0**이다.
별칭에 낮은 가중치를 주면 새로 연결된 이득은 남기고 희석은 줄일 수 있다.
다만 이건 **새 사전등록**이 필요하다. 진 다음에 파라미터를 돌리는 것과
구분되지 않으면 안 되기 때문이다.

## 억제 메커니즘 — 사전 측정 (2026-08-01)

김기인 아이디어: 단순 연결보다 **억제**가 필요하다. 어텐션도 결국 점수를 매겨
중요한 것과 아닌 것을 가르는 일이다.

돌리기 전에 눌러줄 편중이 있는지 쟀다. **내가 처음 제안한 두 형태는 여기서 걸러졌다.**

### 출력 정규화 — 쓸 게 없다

출차수 최대가 HotpotQA 5, 2Wiki 4, MuSiQue 23. 중앙값 1, 90%가 2~3.
"많이 언급하는 문단이 홍수를 낸다"는 그림이 틀렸다. 나눌 분모에 편차가 없다.

### 허브 억제 — 방향이 데이터셋마다 반대다

입차수는 확실히 치우쳤다(MuSiQue 최대 1208, 상위 1%가 엣지의 68%).
그런데 **정답과 잡음의 입차수를 비교하면**:

| | 정답 평균 입차수 | 그 외 | 비 |
|---|---|---|---|
| HotpotQA | 3.33 | 2.78 | **1.20 — 정답이 오히려 허브** |
| 2Wiki | 1.58 | 1.93 | 0.82 — 억제가 맞는 방향 |
| MuSiQue | 8.40 | 9.48 | 0.89 — 약하게 맞는 방향 |

HotpotQA에서는 허브 억제가 **정답을 누른다.** 별칭 실험과 같은 분열이다.

### 부산물 — 별칭 실험의 희석 원인을 잘못 짚었었다

출차수 최대가 5인데 홍수가 날 리 없다. 진짜 원인은 **합치기 순서**다.
확산 결과를 먼저 다 넣고 남는 자리를 dense로 채우므로,
엣지가 늘면 활성 노드가 늘고 **좋은 dense 결과가 20위 밖으로 밀린다.**

### 남은 형태 — 질의 게이팅이 진짜 어텐션 유비다

어텐션의 점수 기준은 연결 구조가 아니라 **질의와의 내용 일치**(`softmax(q·k)`)다.
차수는 질문과 무관한 고정값이지만 어텐션 점수는 질문마다 달라진다.

지금 확산은 질문을 완전히 무시한다. 이웃 v가 그 질문과 무관해도 같은 활성을 받는다.
그런데 **v의 임베딩도 질문 임베딩도 이미 있다.**

    지금        cand = act[u] × w × 0.65
    질의 게이트  cand = act[u] × w × 0.65 × g(sim(질문, v))

계산 비용 0(있는 벡터의 내적). 차수와 무관하게 **무관한 이웃만** 누른다.

## G — 질의 게이팅 (2026-08-01) · 주 가설 기각, 부 가설에서 첫 개선

사전등록: 주 가설 HotpotQA 근거2개@10에서 GM > B0. lo=0.30 hi=0.75 floor=0.25 고정.

| | B0 통제군 | G1 질의게이트 | M1 점수합치기 | GM 둘다 |
|---|---|---|---|---|
| HotpotQA | **0.940** | 0.941 | 0.860 | 0.857 |
| 2Wiki | **0.907** | 0.907 | 0.487 | 0.486 |
| MuSiQue | 0.341 | **0.355** | 0.262 | 0.259 |

**주 가설 기각** — GM이 HotpotQA에서 유의하게 뒤짐(6/89, p<0.0001), 안전장치 위반.

### M1 점수합치기는 내 설계 실수였다

모든 데이터셋에서 재앙이고, 2Wiki에서 0.487이 나왔는데 **dense 단독이 0.486**이다.
즉 이건 "다른 합치기"가 아니라 **그래프를 꺼버린 것**이다.
확산 활성(seed점수 × w × 0.65 ≈ 0.5)이 dense 점수(≈0.8)보다 구조적으로 작아
점수로 섞으면 그래프 결과가 전부 아래로 깔린다.

그리고 이건 앞선 진단의 정정이기도 하다. 별칭 실험의 희석 원인을 "합치기 순서"로
짚었는데 **틀렸다.** 확산 우선 합치기는 실수가 아니라 필수다.

### G1 질의 게이팅 — 다섯 번 만에 나온 첫 개선

| | 근거2개@10 | R@20 | McNemar |
|---|---|---|---|
| HotpotQA | 0.940 → 0.941 | 0.980 → 0.981 | 1/0 — 차이 없음 |
| 2Wiki | 0.907 → 0.907 | 0.968 → 0.968 | 0/0 — 완전 동일 |
| **MuSiQue** | 0.341 → **0.355** | 0.673 → **0.716** | **18/4, p=0.0043 → 앞섬** |

`cand = act[u] × w × 0.65 × g(sim(질문, v))`. 추가 계산 비용 0(있는 벡터의 내적).

이 프로젝트에서 **"메커니즘을 더해서" 유의한 개선이 나온 첫 사례**다
(헤비안·문단술어·논항공유·별칭은 전부 무효 또는 유해였다).
그리고 MuSiQue는 우리가 유일하게 HippoRAG에 지던 데이터셋이다.

**부 가설이므로 성과로 바꿔치기하지 않는다.** 다른 시드에서 재현되는지 먼저 본다.

### 새 시드 재현 — 세 번 다 같은 방향

| MuSiQue | 근거2개@10 (B0→G1) | R@20 | McNemar |
|---|---|---|---|
| seed 0 | 0.341 → 0.355 | 0.673 → 0.716 | 18/4, p=0.0043 |
| seed 1 | 0.336 → 0.347 | 0.668 → 0.707 | 15/4, p=0.0192 |
| seed 2 | 0.373 → 0.391 | 0.673 → 0.711 | 23/5, p=0.0009 |

합계 이득 56 / 손실 13. **재현된다.**

R@20이 일관되게 +3.8~4.3%p 오르는 게 기전과 맞는다. 억제는 **깊은 순위의 잡음을
걷어내는** 일이고, 거기가 확산이 무관한 이웃을 퍼뜨리던 자리다.

HotpotQA·2Wiki에서 효과가 0인 것도 같은 설명이다 — 엣지가 노드당 0.35~0.51개로
애초에 퍼질 곳이 거의 없어 누를 것도 없다. MuSiQue만 0.92개로 두 배다.

**정리: 질의 게이팅은 그래프가 넓게 퍼지는 코퍼스에서만 의미가 있다.**
적용 조건이 또 하나 숫자로 특정됐다.

## R — 게이팅 켜고 MuSiQue 재대결 (2026-08-01) · 판정 안 바뀜

동일 풀(5904)·질문·임베더·LLM(Qwen2.5-7B)·프롬프트·top-10. 검색만 다르다.

### 검색

| | R@10 | R@20 | 근거2개@10 |
|---|---|---|---|
| dense | 0.652 | 0.723 | 0.330 |
| graph | 0.686 | 0.721 | 0.394 |
| **graph+gate** | 0.696 | **0.769** | **0.412** |
| hipporag | 0.725 | 0.781 | **0.454** |

graph vs hipporag 34/64 p=0.0032 → HippoRAG 우세
graph+gate vs hipporag 39/60 p=0.0439 → **여전히 HippoRAG 우세**

격차가 6.0→4.2%p로 줄고 p가 0.003→0.044로 올라왔지만 뒤집지 못했다.

### QA — 게이팅 이득이 전이되지 않는다

| | EM | F1 |
|---|---|---|
| dense | 0.164 | 0.249 |
| graph | **0.190** | 0.277 |
| graph+gate | 0.186 | 0.277 |
| hipporag | **0.218** | **0.315** |

**graph vs graph+gate: 이득 6 / 손실 8, p=0.79 — 차이 없음.**

이유가 숫자에 있다. 게이팅 이득은 **R@20에서 +4.8%p, R@10에서는 +1.0%p**다.
그런데 QA는 top-10만 쓴다. **개선된 구간을 LLM이 보지 않는다.**
근거2개@10 +1.8%p는 500문항 중 9문항이라 EM을 못 움직인다.

graph+gate vs hipporag: 35/19 p=0.0402 → HippoRAG 우세.

### 정리 — 재대결은 판정을 못 바꿨다

게이팅은 **검색에서 재현되는 진짜 개선**이지만(3시드 + 여기서 한 번 더),
**종단 정확도로는 전이되지 않는다.** MuSiQue는 검색·QA 모두 HippoRAG이 앞선다.

두 번째 사례다 — 72B에서도 검색 +12.4%p가 QA 2%p로 줄었다.
**검색 지표의 개선이 종단 정확도로 간다고 가정하면 안 된다.**
어디가 개선됐는지(순위 구간)와 파이프라인이 어디를 쓰는지(top-k)를 맞춰봐야 한다.

미확인: 게이팅 이득이 실제로 있는 구간인 **top-20에서 QA를 재면** 다를 수 있다.
다만 이건 진 뒤에 조건을 고르는 것과 구분돼야 하므로 별도 사전등록이 필요하다.

## 계산뇌과학 형식 — 사전 측정 (2026-08-02)

김기인 제안: 검증된 뉴런 확산 모델을 결합하자.

우리 확산은 여러 경로로 도달해도 **최댓값만 남긴다**(`cand <= act[v]`).
seed 3개에서 도달한 문단이 1개에서 도달한 것과 점수가 같다 — 수렴 증거를 버린다.
실제 뉴런은 적분한다. ACT-R 활성 방정식도 합이다:

    A_i = B_i + Σ_j W_j · S_ji ,   S_ji = S − ln(fan_j)

두 항을 따로 쟀다. **결과가 갈렸다.**

### 합산 항 — 변별력 없음

도달한 노드가 몇 개 seed에서 왔나 (seed 자신 제외):

| | 정답 평균 | 그 외 평균 | 비 |
|---|---|---|---|
| HotpotQA | 1.52 | 1.44 | 1.06 |
| 2Wiki | 1.20 | 1.16 | 1.03 |
| MuSiQue | 1.66 | 1.70 | **0.98** |

정답과 잡음이 **같은 수의 seed에서 도달한다.** 합산하면 둘 다 올라 순위가 안 바뀐다.
MuSiQue는 오히려 잡음이 더 수렴한다. seed 2개 이상에서 도달한 정답이 있는 질문도
5.8 / 11.1 / 11.7%뿐이다.

### fan 할인 — 세 데이터셋 모두 일관되게 유리

도달 경로 **출처의 fan**(출차수):

| | 정답 경로 | 그 외 경로 | 비 |
|---|---|---|---|
| HotpotQA | 1.34 | 1.73 | **0.78** |
| 2Wiki | 1.17 | 1.50 | **0.78** |
| MuSiQue | 2.27 | 2.98 | **0.76** |

**정답에 이르는 경로의 출처가 22~24% 적게 뻗어 있다.** 세 데이터셋에서 값이 거의 같다.

### 앞선 내 기각이 틀렸다

"출력 정규화는 출차수 최대가 5라 편차가 없어 쓸 게 없다"고 적었는데,
**원 분포만 보고 정답/잡음 대비를 안 봤다.** 분포가 좁아도 대비는 일관되게 있다.
허브 억제(입차수 기반)는 데이터셋마다 부호가 뒤집혔지만,
fan 할인(출처의 출차수 기반)은 세 번 다 같은 방향이다. **다른 양이다.**

## F — ACT-R fan 할인 (2026-08-02) · **주 가설 기각**

사전등록: 주 가설 MuSiQue 근거2개@10에서 F1 > B0, S=3.0 고정.

| MuSiQue | 근거2개@10 | McNemar |
|---|---|---|
| B0 통제군 | 0.341 | — |
| F1 ACT-R `(S−ln(1+fan))/S` | 0.338 | 9/12, p=0.66 — 차이 없음 |
| F2 예산 보존 `1/fan` | 0.335 | 11/17, p=0.34 — 차이 없음 |
| F3 ACT-R+게이팅 | **0.359** | 23/5, p=0.0009 — 앞섬 |

HotpotQA·2Wiki는 불일치 0~3건으로 **사실상 완전히 동일**하다.

F3이 유의한 것은 **게이팅 덕분이다.** 게이팅 단독이 같은 시드에서 0.355였고
F3이 0.359인데 둘 사이 대응 검정은 하지 않았다. 차이가 있다고 말할 수 없다.

### 오늘 가장 중요한 방법론적 교훈

사전 측정에서 fan 대비가 세 데이터셋 모두 **0.76~0.78로 놀랄 만큼 일관**됐다.
그런데 순위에 넣으니 **아무 일도 일어나지 않았다.**

> **정답과 잡음을 가르는 특징이 있다**는 것과
> **그 특징을 쓰면 순위가 좋아진다**는 것은 다른 명제다.

fan 정보가 이미 다른 항(dense 점수, 홉 감쇠)에 중복돼 있거나 상대적으로 너무 약했다.
사전 측정은 **명백한 실패를 걸러주지만 성공을 보장하지 않는다.**
(논항 공유는 사전 측정에서 걸러진 게 맞았고, fan은 통과했는데도 실패했다.)

## 현재 시도 누계

| 시도 | 결과 |
|---|---|
| seed 희소성 (빼기) | **성공** — 알고리즘의 핵심 |
| 질의 게이팅 (빼기) | **성공** — MuSiQue, 4회 재현 |
| 헤비안 강화 (더하기) | 기각 — 이득 상한 구조적 0 |
| 문단 술어·극성 (더하기) | 기각 — 다홉에서 유해 |
| 논항 공유 엣지 (더하기) | 사전 측정에서 기각 — lift 1/20 |
| 제목 별칭 엣지 (더하기) | 기각 — 기전은 맞으나 희석이 이김 |
| 점수 기준 합치기 | 기각 — 그래프를 꺼버림 |
| ACT-R 합산 (더하기) | 사전 측정에서 기각 — 변별력 없음 |
| ACT-R fan 할인 | 기각 — 대비는 있으나 순위 불변 |

**성공한 둘은 모두 빼는 메커니즘이고, 더하는 시도는 일곱 번 모두 실패했다.**

## 측면 억제·탈상관 — 사전 측정에서 기각 (2026-08-02)

앞서 시도한 "허브 억제"는 차수 기준이었다. **실제 측면 억제는 유사도 기준**이고
목적은 탈상관이다. 우리 순위에는 다양성 항이 없고, 지표(근거 전부가 상위 10에)는
본질적으로 커버리지라 중복 문단은 칸만 먹는다. 말은 되는데, 상한부터 쟀다.

### 상위 20개 재배열의 하드 상한

| | 이미 맞음 | **재배열로 고칠 수 있음** | top-20에도 없음 |
|---|---|---|---|
| HotpotQA | 94.0% | **2.0%** | 4.0% |
| 2Wiki | 90.7% | **1.1%** | 8.2% |
| MuSiQue | 34.1% | **4.7%** | **61.2%** |

**완벽한 오라클이 상위 20개를 재배열해도 최대 2.0 / 1.1 / 4.7%p다.**
MMR·측면억제·어떤 재순위든 이 안에서 일부만 가져간다.

### 신호와 여지가 어긋난다

실패 질문의 top-10 중복도 ÷ 성공 질문:

| | 비 | 유사도>0.85 쌍 수(실패 질문) |
|---|---|---|
| HotpotQA | 1.005 — 차이 없음 | 0.35개 |
| **2Wiki** | **1.094 — 탈상관이 맞는 방향** | 1.73개 |
| MuSiQue | 1.001 — 차이 없음 | **0.02개** |

**2Wiki는 신호가 맞는데 여지가 1.1%뿐이고, MuSiQue는 여지가 가장 큰데 중복이 없다.**
신호가 있는 곳엔 자리가 없고 자리가 있는 곳엔 신호가 없다.

(부수: 정답끼리 유사도 0.55~0.63 < 정답-오답 0.68~0.72. 다양성 항이 정답을 깎지는
않을 것이다. 다만 상한 때문에 의미가 없다.)

### 이 측정이 바꾸는 것 — 앞으로의 방향

**MuSiQue 질문의 61.2%는 상위 20개 안에 정답이 다 들어오지도 못한다.**
재순위로 손댈 수 없다. 남은 병목은 **순서가 아니라 회수**다.

그리고 회수 병목은 이미 특정돼 있다 — 근거쌍의 37%만 어떤 홉으로도 도달 가능하고
dense가 이미 33%를 가져온다. 나머지는 **코퍼스에 연결 자체가 없다.**
MuSiQue는 서로 다른 문서의 단문 질문을 합성해 만든 데이터라 그렇게 설계된 것이다.

> **재순위 계열 아이디어는 여기서 소진됐다.** 상한이 1~5%p다.
> 이후 작업은 후보 생성(회수)을 겨냥해야 하고, 그건 코퍼스의 링크 구조가
> 결정한다 — 우리가 측정할 수는 있어도 만들어낼 수는 없는 성질이다.

## S — SAM식 단서 갱신 (2026-08-02) · **주 가설 기각**

김기인 방향: 시각계의 측면 억제 말고 **논리 회로·생각의 흐름**.
형식은 SAM(Raaijmakers & Shiffrin 1981) — 회상한 항목이 단서에 합류해 다음 표집을 바꾼다.
반복 인출 자체는 IRCoT·Self-Ask·FLARE가 이미 한다. **차이는 LLM을 안 쓴다는 것**
(단서 = `unit(q + p_top1)`, 벡터 합).

### 사전 측정 — 여지가 지금까지 잰 것 중 가장 컸다

놓친 정답을 새 단서로 되찾은 비율 (`job_cue.py`):

| | 대상 질문 | 오라클 | **실제 조건(1위 결과 사용)** |
|---|---|---|---|
| HotpotQA | 4.0% | +2.6%p | +1.9%p |
| 2Wiki | 8.2% | +4.1%p | +4.2%p |
| **MuSiQue** | **57.6%** | +13.5%p | **+13.4%p** |

**오라클과 실제 조건이 같다** — 어느 문단이 진짜 발판인지 알 필요가 없다.
MuSiQue에서 발판이 없는 질문은 4.3%뿐이다. 95.7%는 정답을 하나는 이미 찾고 있고
**못 가는 건 두 번째 홉뿐이다.**

### 실행 결과 — 기각

| | 근거2개@10 | R@20 | McNemar |
|---|---|---|---|
| MuSiQue | 0.341 → 0.349 | 0.673 → **0.698** | 15/7, p=0.13 — 유의하지 않음 |
| HotpotQA | 0.940 → 0.938 | 0.980 → **0.985** | 12/14, p=0.85 — 차이 없음 |
| 2Wiki | 0.907 → **0.820** | 0.968 → **0.976** | 5/92, p<0.0001 — **뒤짐** |

2Wiki에서 안전장치 위반(R@10 −2.3%p).

### 기전은 완벽히 작동했다. 합치기가 죽였다.

| | 일부만 찾은 질문(표적) | 이미 전부 찾은 질문 |
|---|---|---|
| MuSiQue | 이득 3 / **손실 0** | 이득 12 / 손실 7 |
| HotpotQA | 이득 1 / **손실 0** | 이득 11 / 손실 14 |
| 2Wiki | 이득 1 / **손실 0** | 이득 4 / **손실 92** |

**표적 구간에서 손실이 0건이다.** 2라운드는 정확히 의도한 자리에서만 작동한다.
피해는 전부 **이미 맞고 있던 질문**에서 났다.

2Wiki가 무너진 이유는 특정된다. **2Wiki는 R@5가 0.714인데 R@10이 0.962다 —
정답이 6~10위에 몰려 있다.** 그런데 사전등록한 합치기가 정확히 그 구간을 RRF로
재배열했다. 제자리에 있던 정답을 직접 흔든 것이다.

### 사전 측정과의 격차 — 병목은 찾기가 아니라 예산

MuSiQue 대상 질문 563개 중 이득이 **3개**다. 상한은 13.4%p(약 130문항)였다.
2라운드가 정답을 **찾아오기는 하는데 top-10에 앉힐 자리가 없다.**

증거: **R@20은 세 데이터셋 모두 올랐다**(+2.5 / +0.5 / +0.8%p).
2라운드의 수확이 11~20위에 쌓이고 있다.

> 이걸로 세 번째다 — 별칭 엣지, 점수 합치기, 단서 갱신.
> **새로 찾은 것을 기존 상위권에 끼워 넣는 비용이 매번 이득을 넘었다.**
> `all@10`은 칸이 10개뿐이고 이미 정답이 차지하고 있다.

### 미확인 (새 사전등록이 필요)

R@20이 일관되게 오른 것은 관찰이지 주장이 아니다. 이걸 쓰려면
**리더가 20개를 읽는 설정**에서 QA를 재야 하고, 그건 컨텍스트 비용이 두 배다.
NEXT.md의 B1과 같은 항목이며, 그쪽 상한(2.0/1.1/4.7%p)과 함께 판단해야 한다.

## [정정 2026-08-02] "재순위 소진" 주장이 틀렸다

`job_redundancy.py`에서 "완벽한 오라클이 재배열해도 1~5%p"라고 적고 재순위 계열을
접었다. **그건 상위 20개를 재배열했을 때의 상한이었다.** 후보를 깊게 뽑는 건 다른
문제이고 재본 적이 없었다.

### 깊이별 근거 전부 포함률

| k | HotpotQA dense/graph | 2Wiki dense/graph | MuSiQue dense/graph |
|---|---|---|---|
| 10 | 0.857 / 0.940 | 0.486 / 0.907 | 0.259 / 0.341 |
| 20 | 0.911 / 0.960 | 0.518 / 0.918 | 0.363 / 0.388 |
| 50 | 0.949 / 0.983 | 0.583 / 0.931 | 0.477 / 0.534 |
| 100 | 0.962 / 0.988 | 0.628 / 0.942 | 0.559 / **0.647** |
| 200 | 0.983 / 0.991 | 0.687 / 0.954 | 0.646 / **0.732** |
| 500 | 0.996 / 0.997 | 0.803 / 0.973 | 0.738 / 0.808 |

**MuSiQue에서 top-100 재순위의 상한이 +30.6%p다.** 오늘 잰 어떤 여지보다 한 자릿수 크다
(재배열 4.7%p, 단서 갱신 13.4%p).

정답이 없어서 못 찾은 게 아니다. **11~100위에 있는 걸 우리가 안 본 것이다.**

그리고 **그래프 우위가 깊이에서도 유지된다** — top-100에서 0.647 vs 0.559(+8.8%p).
깊은 후보 생성에도 값이 있다.

### 교훈

상한을 잴 때 **무엇의 상한인지**를 정확히 적어야 한다. "재배열의 상한"을
"재순위의 상한"으로 적었고, 그 한 단어 차이로 가장 큰 여지를 두 번 지나쳤다.
공개 README에도 그대로 적혀 있었으므로 함께 정정했다.

## K — 깊은 후보 + 크로스인코더 재순위 (2026-08-02) · **주 가설 기각**

사전등록: 주 가설 MuSiQue 근거2개@10에서 R1 > B0. 후보 100개, `BAAI/bge-reranker-v2-m3`.

| 근거2개@10 | B0 통제군 | R1 그래프+재순위 | R2 dense+재순위 |
|---|---|---|---|
| HotpotQA | **0.940** | 0.915 | 0.905 |
| 2Wiki | **0.907** | **0.467** | 0.452 |
| MuSiQue | **0.341** | 0.311 | 0.290 |

R1 vs B0 — HotpotQA 38/63 p=0.017 뒤짐 / 2Wiki 5/445 p<0.0001 뒤짐(안전장치 위반,
R@10 −20.9%p) / MuSiQue 96/126 p=0.051.

**세 데이터셋 모두 나빠졌다.** 후보를 깊게 뽑은 것 자체는 맞았지만(상한 +30.6%p),
크로스인코더로 재순위하면 그 이상을 잃는다.

### 기전이 완벽하게 분리된다

| | 정답이 깊이(11~100위) 있었음 | 이미 top-10에 다 있었음 |
|---|---|---|
| MuSiQue | 이득 96 / **손실 0** (n=659) | **이득 0** / 손실 126 (n=341) |
| HotpotQA | 이득 38 / **손실 0** (n=60) | **이득 0** / 손실 63 (n=940) |
| 2Wiki | 이득 5 / **손실 0** (n=93) | **이득 0** / 손실 445 (n=907) |

**재순위기는 깊이 묻힌 정답에서 절대 지지 않고, 이미 맞은 질문에서 절대 이기지 않는다.**
순수한 맞교환이고, 세 데이터셋 모두 손해가 더 크다.

### 원인 — 다리 문단은 정의상 질문과 안 닮았다

R@1·R@5는 모두 올랐다(HotpotQA 0.869→0.926, MuSiQue 0.537→0.559).
**근거2개@10만 떨어졌다.** 크로스인코더는 질문-문단 **직접 관련성**을 점수화하는데,
멀티홉의 두 번째 홉 문단은 질문에 나오는 개체를 담고 있지 않다.
"X 감독의 어머니는?"에서 어머니 문서에는 X가 없다.

2Wiki가 0.907 → 0.467로 무너진 게 가장 순수한 사례다. 템플릿 질문이라
다리 문단과 질문의 어휘 겹침이 거의 0이다.

> **점별 관련성을 최적화하는데 지표는 결합 커버리지다.** 목적 함수가 다르다.
> "RAG에는 재순위기를 붙여라"가 기본 조언이지만, **멀티홉에서는 해롭다.**

### 우리 방법에는 중요한 긍정 결과

**R1 vs R2 — 세 데이터셋 모두 그래프가 유의하게 기여한다:**
MuSiQue 25/4 (p=0.0001), HotpotQA 10/0 (p=0.0020), 2Wiki 15/0 (p=0.0001).
**손실이 한 건도 없다.** 재순위기를 붙인 뒤에도 그래프가 값을 더한다 —
우리 방법이 표준 현대 파이프라인 안에서 살아남는다는 첫 확인이다.

### 다섯 번째 같은 패턴, 그리고 구조적 결론

별칭 · 점수합치기 · 단서갱신 · 재순위 — 전부 **표적 구간에서 손실 0, 다른 구간에서 손해**.

이제 이유가 분명하다. **`all@10`은 칸이 10개뿐인 제로섬이다.**
새 정답을 넣으려면 있던 것을 빼야 하는데, 이미 맞고 있던 질문에서 그건 정답이다.
**찾는 문제는 여러 번 풀었고, 앉히는 문제는 한 번도 못 풀었다.**

빠져나갈 길은 둘뿐이다 — 예산을 늘리거나(리더가 20개를 읽음, 컨텍스트 2배),
**문단이 아니라 집합을 점수화하는 재순위기**를 쓰거나. 후자는 이 프로젝트 범위 밖이다.

## M — 작은 리더로 큰 리더 효과 내기 (2026-08-02)

목표(김기인): **4~7B를 추론에 쓰면서 70B급 효과, 그리고 빠르게.**
검색·문단·질문·top-k를 완전히 고정하고 **리더 쪽만** 바꾼다.

출발점의 문제: 우리 프롬프트가 `"Reply with the short answer span only — no explanation"`으로
**추론을 명시적으로 금지하고 있었다.** 큰 모델은 멀티홉을 속으로 처리하지만
작은 모델은 밖으로 꺼내야 한다. 그동안 7B를 불리하게 쓰고 있었던 것이다.

| Qwen2.5-7B, top-10, n=500 | S0 현재 | S1 CoT | S2 자기일관성 | **S3 CoT+SC** | 72B 기준 | 격차 메움 |
|---|---|---|---|---|---|---|
| HotpotQA | 0.546 | 0.560 | 0.538 | **0.572** | 0.612 | **39%** |
| **2Wiki** | 0.436 | 0.572 | 0.426 | **0.578** | 0.532 | **148%** |
| MuSiQue | 0.190 | 0.216 | 0.190 | **0.232** | 0.238 | **88%** |

F1도 같은 방향이다 (HotpotQA 0.686→0.727, 2Wiki 0.532→0.679).

### 두 기법은 곱해진다

HotpotQA에서 CoT 단독 +21%, 자기일관성 단독 **−12%**, 합치면 **+39%**다.

자기일관성은 **투표할 추론 경로가 있어야** 작동한다. S2는 짧은 답만 뽑으니
투표할 것이 없어 온도만 올린 꼴이고, 세 데이터셋 모두 S0보다 나쁘다.
CoT를 붙이면 서로 다른 경로가 생겨 다수결이 의미를 갖는다.
**둘은 함께 써야 하고, 자기일관성 단독은 쓰면 안 된다.**

### 2Wiki에서 7B가 72B를 넘었다 — 단서를 붙여서

0.578 vs 0.532. CoT 하나만으로 0.436 → 0.572(+13.6%p)다.

2Wiki는 "X 감독의 어머니는?" 같은 **명시적 합성 템플릿**이라 중간 단계를 글로 쓰면
바로 풀린다. CoT가 정확히 그 자리를 친다. HotpotQA는 비교형이 섞여 이득이 작고(+1.4%p),
MuSiQue는 홉이 깊어 중간(+2.6%p)이다.

> **단서: 72B 기준값은 CoT 없이 측정된 것이다.**
> 우리 원래 프롬프트로 돌린 값이므로 이건 "7B+CoT가 72B+CoT를 이겼다"가 아니라
> **"7B+CoT가 72B(CoT 없음)를 이겼다"**이다. 72B에도 CoT를 붙이면 그쪽도 오른다.
> 공정한 비교는 별도 실행이 필요하다(a100x4).

실용적으로는 여전히 의미가 있다 — **우리가 실제로 돌리던 72B 구성보다,
10배 작은 모델을 제대로 쓴 쪽이 낫다.** 메모리 14GB vs 144GB다.

### 검색과 리더의 지렛대 크기 비교

| 무엇을 바꿨나 | EM 변화 |
|---|---|
| 검색: dense → 우리 그래프 (2Wiki, 7B) | +0.026 |
| 검색: 우리 그래프 → HippoRAG2 등 (최대) | ~+0.02 |
| **리더: 프롬프트만 CoT+SC로 (2Wiki)** | **+0.142** |

**프롬프트 한 줄이 검색 연구 전체보다 5배 큰 효과를 냈다.**
이 프로젝트가 열 번의 실험을 검색에 쓰는 동안, 리더 쪽은 한 번도 안 건드렸다.

### MuSiQue 완료 — 사전등록 주 가설 **통과**

MuSiQue S3 vs S0: 이득 36 / 손실 15, **p=0.0046 → 유의**.
사전등록한 주 가설("MuSiQue EM에서 S3 > S0")이 통과했다.
**이 프로젝트에서 주 가설이 통과한 첫 실험이다.**

### 자기일관성에 대한 권고 수정

앞에서 "8배 비용에 1%p라 빼라"고 적었는데, **MuSiQue에서는 결정적이다.**

| 자기일관성이 추가로 메운 격차 | |
|---|---|
| 2Wiki | +6%p (142% → 148%) |
| HotpotQA | +18%p (21% → 39%) |
| **MuSiQue** | **+34%p (54% → 88%)** |

MuSiQue에서는 **S1 단독이 유의하지 않고(36/23, p=0.1175) S3만 유의하다(p=0.0046).**

> **추론이 어려울수록 투표가 값을 한다.**
> 쉬운 템플릿 질문은 CoT 한 번에 풀리고, 3~4홉은 경로가 갈려 다수결이 걸러준다.
> 권고는 "항상 빼라"가 아니라 **난이도에 따라** — 2Wiki류면 CoT만, MuSiQue류면 CoT+SC.

### 비용 계산 정정

앞서 "7B+SC가 72B보다 15배 더 연산한다"고 적었는데 **디코드만 세고 prefill을 뺐다.**
RAG는 문단 10개를 읽어 입력이 ~2000토큰이고, **그 입력을 읽는 비용이 지배적이다.**

| 질의 1건당 총 연산 | prefill | 디코드 | 합계 |
|---|---|---|---|
| 72B 평문 | ~288 TFLOPs | ~1 | **~289** |
| 7B + CoT 1회 | ~28 | ~2 | **~30 (10배 저렴)** |
| 7B + CoT 8샘플 | ~28 (프롬프트 공유) | ~17 | **~45 (6배 저렴)** |

**연산도 메모리도 7B가 유리하다.** 긴 문맥 RAG에서는 모델 크기가 prefill에 그대로 곱해진다.

## 폐쇄형 통제 — RAG가 실제로 얼마를 더하는가 (2026-08-03)

우리는 검색 방법들끼리만 비교했지 **검색을 아예 안 했을 때**를 한 번도 안 쟀다.
HotpotQA·2Wiki는 유명 위키 개체 위주라 7B가 이미 알고 있을 수 있고,
그렇다면 우리 검색 연구 전체가 좁은 여지 위에서 논 셈이 된다. 그래서 잰다.

Qwen2.5-7B, n=500, 검색 없이 자기 지식만으로:

| 평문 프롬프트 | 검색 없음 | RAG(우리 그래프) | RAG가 더하는 값 |
|---|---|---|---|
| HotpotQA | 0.200 | 0.546 | **+0.346 (2.7배)** |
| 2Wiki | 0.222 | 0.436 | **+0.214 (2.0배)** |
| MuSiQue | **0.038** | 0.190 | **+0.152 (5.0배)** |

**기반이 튼튼하다.** 검색이 신호의 대부분을 공급한다.

### CoT는 RAG를 대체하지 못한다

폐쇄형에 CoT+자기일관성을 다 붙여도 0.222 / 0.282 / 0.086이다.
**검색만 붙인 평문(0.546 / 0.436 / 0.190)에 한참 못 미친다.**

앞서 "프롬프트가 검색 연구 전체보다 5배 컸다"고 적었는데, 그건
**검색 방법들 사이의 차이**와 비교한 것이다. **검색의 유무**와 비교하면 반대다.
정리하면 지렛대 크기는 이 순서다:

    검색 있음/없음  ≫  리더 프롬프트(CoT)  >  검색 방법 간 차이

(다만 폐쇄형에서도 CoT는 MuSiQue에서 유의하다: 0.038 → 0.086, p=0.0001.
자기 지식 위에서도 단계를 밟으면 낫다. 절대값이 작을 뿐이다.)

## 양자화 — 4비트 3B (2026-08-03)

`Qwen2.5-3B-Instruct-AWQ`, 나머지 조건 동일.

| 3B | fp16 S0 | 4bit S0 | fp16 S3 | 4bit S3 |
|---|---|---|---|---|
| HotpotQA | 0.458 | 0.420 (−3.8%p) | **0.576** | 0.506 (**−7.0%p**) |
| 2Wiki | 0.348 | 0.348 (**0**) | **0.566** | 0.468 (**−9.8%p**) |

### 양자화 손해가 CoT에서 증폭된다

**2Wiki 평문에서는 손해가 정확히 0인데 CoT를 켜면 −9.8%p다.**

짧은 답 하나는 틀릴 기회가 한 번이지만 **CoT는 150토큰짜리 사슬을 전부 맞게 이어가야**
하고 양자화 오차가 그 위에서 누적된다. 추론이 길수록 열화가 증폭된다.

> **양자화 품질을 짧은 출력이나 perplexity로 재면 추론 작업의 실제 손해를 과소평가한다.**
> 같은 모델·같은 데이터에서 0 vs −9.8%p로 갈린다.

이는 BitBudget의 원래 문제의식(양자화 품질 계측의 부적절성)과 직접 이어진다.

다만 **레시피는 양자화 후에도 살아남는다** — 4비트 S3(0.506 / 0.468)가
fp16 평문(0.458 / 0.348)보다 여전히 낫다. 메모리는 1.8GB다.

## 전체 위키 실현 가능성 — 스모크 (2026-08-03)

BEIR HotpotQA 코퍼스(위키피디아 520만 문단) 중 20만으로 검증:

- 엣지 생성 **40초 / 20만 문단** = 문단당 0.2ms → **520만이면 약 17분**
- 노드당 엣지 **8.02개** — 우리 소규모 풀(0.35~0.92)보다 **10배 조밀**
- 같은 규모에서 LLM 기반 그래프 RAG는 약 **52일**

밀도가 10배라는 건 양날이다. 연결은 좋아지지만 확산이 넓게 퍼져
**질의 게이팅(억제)이 여기서 진짜로 필요해질 것**이다 —
게이팅이 MuSiQue(0.92)에서만 효과가 있었던 이유가 밀도였다.

(20만 절단본의 연결률 44.7%는 정답 문단 대부분이 코퍼스 밖으로 나가 편향됐다.
전체 실행 결과로 대체할 것.)

## 전체 위키 실현 가능성 — 확정 (2026-08-03)

BEIR HotpotQA 코퍼스 = **위키피디아 520만 문단**. MTEB 검색 과제에 포함돼 있다.

| | |
|---|---|
| 문단 | 5,233,329 |
| 언급 엣지 | **63,895,815** (노드당 **12.21**) |
| **색인 시간** | **23.9분** (CPU, LLM 0회) |
| **근거쌍 연결률** | **59.0%** (4,372/7,405) |
| LLM 기반 그래프 RAG였다면 | **54일** |

**59.0%는 좋은 범위다.** 소규모 HotpotQA 풀(61.0%)과 거의 같고,
이득이 안 났던 MuSiQue(32.4%)보다 한참 위다. **규모를 키워도 구조가 유지된다.**

노드당 엣지가 소규모 풀(0.35~0.92)의 **13배**다. 예상대로
**질의 게이팅(억제)이 여기서 진짜로 필요해질 것**이다 —
게이팅이 MuSiQue(0.92)에서만 효과가 있었던 이유가 밀도였다.

> 위키피디아 전체를 **24분에 색인**한다. 같은 자리에서 LLM 기반 그래프 RAG는 54일이다.
> 그 계열 논문이 전부 수천~수만 문단 풀에서만 보고하는 이유가 이것이다.

## FRAMES — 코퍼스 확보는 절반만 (2026-08-03)

FRAMES(구글, 2024)는 질문 824개와 **위키 링크**만 주고 본문을 안 준다.
`wikimedia/wikipedia` 20231101.en(41조각)에서 제목으로 붙였다.

- 제목 매칭 **2,025/2,479 (81.7%)**
- **근거를 전부 확보한 질문 466/824 (56.6%)** — 하나라도 빠지면 못 쓴다

### 탈락이 무작위가 아니다 — 쓸 때 반드시 붙일 단서

못 찾은 제목이 `'1580'`, `'1901'`, `'1936 Summer Olympics'`,
`'1984 Summer Olympics medal table'` 같은 **연도·표 문서**에 몰려 있다.
즉 **표 추론·시간 추론 문제가 우선적으로 탈락**했다.

남은 466문항은 원본보다 **쉬운 쪽으로 편향**된다.
**공개된 FRAMES 점수와 비교하면 안 된다.** 내부 비교(dense vs 그래프)에는 쓸 수 있지만
"FRAMES에서 몇 점"으로 보고하면 오해를 만든다.

(살아 있는 위키 API로 나머지를 채우면 커버리지는 오르지만 재현이 깨진다.
날짜 고정 덤프를 택한 대가다.)
