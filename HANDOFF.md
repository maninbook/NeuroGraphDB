# 인수인계 — 다음 세션은 여기부터

작성 2026-08-06. 코랩 MCP 연결을 위해 세션을 재시작하며 남긴다.

## 지금 어디에 있는가

전체 1000, Recall@5, 공식 코퍼스(`osunlp/HippoRAG_v2`) + NV-Embed-v2, 정답은 문단 인덱스 기준.

| | 우리 | HippoRAG 2 | PropRAG |
|---|---|---|---|
| MuSiQue | 74.51 | 74.7 | 78.3 |
| 2Wiki | **94.92** | 90.4 | 94.1 |
| HotpotQA | 96.35 | 96.3 | 97.4 |
| 평균 | **88.59** | 87.1 | 89.9 |

**색인에 LLM을 한 번도 쓰지 않는다.** LLM은 질문 1,000개의 후속 질의 생성에만 쓰고,
그것도 로컬 `llama3.1:8b`(4비트)다. 전부 무료로 재현된다.

채택 설정: 1홉 4칸 + 후속질의 1칸 `(4,1,0)`. dev(짝수 500)에서 고르고 test(홀수 500)에서 쟀다.

## 바로 할 일 — 코랩 임베딩

`colab-proxy-mcp`를 방금 붙였다. 이 세션에서 처음 쓴다.

1. `open_colab_browser_connection` 호출 → 브라우저가 열린다.
   **구글 로그인은 사용자가 직접** 한다(자격증명은 다루지 않는다).
2. 런타임 **L4 또는 A100**. T4(16GB)는 fp16 가중치 15.7GB만으로 꽉 차서 실패한다.
3. `colab/embed_nvembed.ipynb`를 열어 실행. 예상 30분, 2~3 컴퓨팅 단위.
4. 마지막 셀이 **기존 캐시와의 코사인**을 잰다. **0.999 이상이어야 한다.**
   아니면 인코딩 설정이 어긋난 것이니 그대로 진행하면 안 된다.

만드는 것:
- `emb/{musique,2wiki,hotpotqa}_followups_r{1,2}_NV-Embed-v2.npy`
- `emb/popqa_NV-Embed-v2_{P,Q}.npy`

### 왜 필요한가

**2홉을 BM25 → dense로.** 지금 2홉 검색이 BM25인 것은 후속 질의를 임베딩할 방법이
없어서였다(NV-Embed-v2는 7.85B, 로컬 19.3GB에 안 들어간다). 같은 공간에 넣으면
dense로 돌릴 수 있고 그것이 2홉 품질을 직접 올린다.

**PopQA.** PropRAG이 보고하는 네 번째 데이터셋(55.3). 단일홉이라 우리 방법이 힘을 못 쓰고
**평균이 내려갈 가능성이 크다.** 그래도 넣는다 — 빼면 유리한 것만 골랐다는 말을 듣는다.

## 그다음 (전부 로컬 CPU, 무료)

- `neurographdb/local_e5_all.py`의 2홉을 BM25 대신 dense로 바꿔 재평가
- PopQA를 네 번째 데이터셋으로 추가
- 미검증으로 남은 것: **라운드 간 합의 재순위** — hop1·R1·R2가 겹쳐 찾은 문단은
  정답일 확률이 높은데 지금은 위치로만 합쳐 이 신호를 버리고 있다. 칸을 뺏지 않으므로
  §22의 예산 제약에 걸리지 않는다. 새 기제이므로 사전등록부터.

## 반드시 지킬 규칙 (여러 번 데인 것들)

- **dev/test 분할.** 짝수=dev에서 고르고 홀수=test에서 한 번만 잰다.
  test만 보면 안 된다 — §19에서 dev와 test가 부호까지 갈렸다.
- **규칙 A.** 세 데이터셋 **전부**에서 −0.3%p 넘게 떨어지면 기각. 평균으로 판단하지 않는다.
- **안전장치.** 기제를 끈 설정이 기록값(72.98 / 93.50 / 96.15)을 정확히 재현해야 한다.
- **lift로 관문을 세우지 않는다.** §17 — lift는 비율이지 정밀도가 아니다.
  새 엣지 기제는 **기존 엣지 집합보다 정밀도가 높아야** 한다.
- **프롬프트를 결과 보고 고치지 않는다.** 그것이 과적합이다.
- **전체 1000 수치로 보고한다.** 경쟁 수치가 그 기준이다. test 500 값으로 "이겼다"고 쓰지 않는다.
- **test는 오늘 여러 번 읽어 닳았다.** +0.3 수준 이득은 주장하지 않는다.

## 기록 위치

- `SOTA.md` §12~§22 — 오늘의 전부. 기제 13개 검증, 12개 기각, 1개 채택.
- `RESULTS.md` — 그 이전 연구 일지
- Hub `goethe0101/neurographdb-results` — 임베딩·후속질의·진단 JSON 전부
- 로컬 `neurographdb/cache/` — 후속 질의 R1·R2 (Hub에도 올라가 있다)

## 환경 주의

- HF Jobs·인퍼런스는 **크레딧 소진(402)**. 무료 티어 없음. 다운로드/업로드만 가능.
- ollama는 `ollama serve`로 직접 띄워야 할 때가 있다. `llama3.1:8b` 필요.
- C++ 코어는 로컬 python 3.12용으로 빌드돼 있다.
  다시 빌드하려면 `-undefined dynamic_lookup`이 필요하다(macOS).
- 세션이 바뀌면 백그라운드 프로세스가 죽는다. 긴 생성은 **캐시를 디스크+Hub에 즉시** 저장할 것.
